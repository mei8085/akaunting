# Akaunting 交易更新权限路径反向验证报告

> **验证场景**：目标交易已关联账单（`transaction.document_id != null`），但请求体**不带** `document_id` 参数

---

## 1. 三条更新路径完整分析

### 1.1 路径一：Web 控制器更新

**调用入口**：
- 路由：`PUT /transactions/{transaction}`
- 控制器：`app/Http/Controllers/Banking/Transactions.php:248-280`

**拦截逻辑**：
```php
public function update(Transaction $transaction, Request $request)
{
    // 检查目标交易本身是否已关联账单
    if ($transaction->document_id) {
        $message = trans('transactions.messages.update_document_transaction');
        
        flash($message)->error()->important();
        
        return response()->json([
            'success' => false,
            'error' => true,
            'message' => $message,
            'redirect' => route('transactions.edit', $transaction->id),
        ]);
    }

    $response = $this->ajaxDispatch(new UpdateTransaction($transaction, $request));
    // ...
}
```

**拦截点**：
- 位置：控制器层（进入 Job 之前）
- 检查对象：**目标交易的现有状态**（`$transaction->document_id`）
- 检查逻辑：只要交易已关联账单，无论请求体带不带 `document_id`，一律拦截

**场景下行为**：
- 目标交易 `document_id = 123`（已关联账单）
- 请求体：`{ "amount": 500, "description": "修改测试" }`（不带 `document_id`）
- ✅ **被拦截**，返回错误：`transactions.messages.update_document_transaction`

---

### 1.2 路径二：API 通用控制器更新

**调用入口**：
- 路由：`PUT /api/transactions/{transaction}`
- 控制器：`app/Http/Controllers/Api/Banking/Transactions.php:62-71`

**拦截逻辑**：
```php
public function update(Transaction $transaction, Request $request)
{
    // 检查请求体中是否包含 document_id 参数
    if ($request->has('document_id')) {
        return $this->errorBadRequest(trans('transactions.messages.update_document_transaction_error'));
    }

    $transaction = $this->dispatch(new UpdateTransaction($transaction, $request));

    return new Resource($transaction->fresh());
}
```

**拦截点**：
- 位置：控制器层（进入 Job 之前）
- 检查对象：**请求体参数**（`$request->has('document_id')`）
- 检查逻辑：只有当请求体中显式包含 `document_id` 字段时才拦截，不检查交易的现有状态

**场景下行为**：
- 目标交易 `document_id = 123`（已关联账单）
- 请求体：`{ "amount": 500, "description": "修改测试" }`（不带 `document_id`）
- ❌ **不被拦截**，直接进入 `UpdateTransaction` Job

**Job 层授权检查** (`app/Jobs/Banking/UpdateTransaction.php:65-76`):
```php
public function authorize(): void
{
    // 只检查这两个条件
    if ($this->model->reconciled) {
        $message = trans('messages.warning.reconciled_tran');
        throw new \Exception($message);
    }

    if ($this->model->isTransferTransaction()) {
        throw new \Exception(trans('messages.error.transfer_transaction'));
    }
    
    // 注意：完全没有检查 document_id！
}
```

**最终结果**：
- ✅ **更新成功**！已关联账单的未对账交易可以通过 API 通用接口修改
- ⚠️ **但不会触发账单状态重算**（因为走的是 `UpdateTransaction` 而非 `UpdateBankingDocumentTransaction`）
- 账单的 `status` 和 `paid` 都不会更新，造成数据不一致

---

### 1.3 路径三：API 文档专用控制器更新

**调用入口**：
- 路由：`PUT /api/documents/{document}/transactions/{id}`
- 控制器：`app/Http/Controllers/Api/Document/DocumentTransactions.php:83-92`

**拦截逻辑**：
```php
public function update($document_id, $id, Request $request)
{
    $document = Document::find($document_id);

    // 确保交易属于该文档
    $transaction = Transaction::documentId($document_id)->find($id);

    // 无任何 document_id 检查，直接调用专用 Job
    $transaction = $this->dispatch(new UpdateBankingDocumentTransaction($document, $transaction, $request));

    return $this->created(route('api.documents.transactions.show', [$document_id, $transaction->id]), new Resource($transaction));
}
```

**拦截点**：
- 位置：无（无 `document_id` 拦截）
- 检查对象：通过 `documentId()` scope 确保查询到的交易确实关联该文档
- 设计意图：专门用于更新已关联文档的交易

**场景下行为**：
- 目标交易 `document_id = 123`（已关联账单）
- 请求体：`{ "amount": 500, "description": "修改测试" }`（不带 `document_id`）
- ✅ **正常处理**，进入 `UpdateBankingDocumentTransaction` Job

**专用 Job 处理** (`app/Jobs/Banking/UpdateBankingDocumentTransaction.php`):
1. `prepareRequest()`: 自动补全 `document_id` 等字段
2. `checkAmount()`: 先减去当前交易金额，重新计算未付金额，更新账单状态
3. 调用 `UpdateTransaction` 完成实际更新
4. 创建文档历史记录

**最终结果**：
- ✅ **更新成功**
- ✅ **账单状态正确重算**

---

## 2. 三层口径差异完整矩阵

| 检查维度 | Web 控制器 | API 通用控制器 | API 文档专用控制器 |
|---------|-----------|---------------|-------------------|
| **调用路由** | `PUT /transactions/{id}` | `PUT /api/transactions/{id}` | `PUT /api/documents/{doc_id}/transactions/{id}` |
| **控制器位置** | `Banking\Transactions::update` | `Api\Banking\Transactions::update` | `Api\Document\DocumentTransactions::update` |
| **拦截检查对象** | 目标交易现有状态 `$transaction->document_id` | 请求体参数 `$request->has('document_id')` | 无（通过 `documentId()` scope 过滤） |
| **请求不带 document_id 时** | ❌ 拦截（无论请求如何，只要交易已关联就拦截） | ✅ 放行（请求中没有这个参数就通过） | ✅ 放行（专用接口，设计允许） |
| **请求带 document_id 时** | ❌ 拦截 | ❌ 拦截 | ✅ 放行 |
| **Job 层是否检查 document_id** | -（被控制器拦截，不会进入） | ❌ 不检查（Job 只检查 reconciled 和 transfer） | ✅ 专用 Job，不检查但正确处理 |
| **账单状态是否重算** | - | ❌ 不会（走 UpdateTransaction） | ✅ 会（走 UpdateBankingDocumentTransaction） |

---

## 3. 核心代码理解错误修正

### 3.1 之前的错误理解

> **错误观点**：API 通用控制器检查 `document_id` 的目的是防止修改已关联账单的交易。

### 3.2 实际代码行为

**检查对象完全错误**：
- Web 控制器检查的是**交易本身的状态**（`$transaction->document_id`）
- API 通用控制器检查的是**请求体参数**（`$request->has('document_id')`）

**语义差异**：
| 检查方式 | 语义 | 实际效果 |
|---------|------|---------|
| `$transaction->document_id` | "交易是否已关联账单" | 保护已关联的交易不被修改 |
| `$request->has('document_id')` | "请求是否尝试设置 document_id" | 防止通过通用接口建立关联，但不保护已有关联 |

### 3.3 API 通用控制器的设计意图推测

从代码看，API 通用控制器的检查目的是：
- 防止用户通过通用交易更新接口**建立或修改**账单关联
- 要求关联操作必须通过文档专用接口进行

但它**没有**检查交易是否**已经**关联账单，这是一个严重的设计疏漏。

---

## 4. 可复现实验步骤

### 实验一：Web 端更新被拦截（验证 Web 控制器行为）

**前置条件**：
- 已创建账单：ID=100，金额=$1000，状态=`sent`
- 已创建交易：ID=200，金额=$500，`document_id=100`，`reconciled=0`，非转账交易

**操作步骤**：
1. 登录 Web 界面，导航到交易列表
2. 找到交易 #200，点击"编辑"
3. 修改金额为 $600，点击"保存"

**预期结果**：
- ✅ 保存失败，显示错误信息：`This transaction is linked to a document. You can't edit it.`（或对应翻译）
- 交易金额仍为 $500
- 账单状态仍为 `partial`

**验证代码**：
```php
// 在 tinker 中验证
$transaction = \App\Models\Banking\Transaction::find(200);
echo $transaction->document_id;  // 100（已关联）
echo $transaction->amount;       // 500（未被修改）
```

---

### 实验二：API 通用更新成功（验证 API 通用控制器漏洞）

**前置条件**：同实验一

**操作步骤**：
1. 获取 API Token
2. 发送 PUT 请求（注意：请求体**不带** `document_id`）：
```http
PUT /api/transactions/200
Content-Type: application/json
Authorization: Bearer {token}

{
    "type": "income",
    "number": "TXN-200",
    "account_id": 1,
    "paid_at": "2026-05-20 00:00:00",
    "amount": "600",
    "currency_code": "USD",
    "currency_rate": "1",
    "category_id": 1,
    "payment_method": "offline-payment.cash",
    "description": "API 修改测试"
}
```

**预期结果**：
- ✅ HTTP 200 OK，返回更新后的交易资源
- 交易金额变为 $600
- ⚠️ 账单状态**仍为** `partial`（未重算）
- ⚠️ 账单 `paid` 动态属性**仍为** $500（因为 `status != 'paid'` 时会动态计算，但关联的交易金额已变，实际应该是 $600）
- ❌ **数据不一致**：交易金额已变，但账单支付金额和状态未更新

**验证代码**：
```php
// 在 tinker 中验证
$transaction = \App\Models\Banking\Transaction::find(200);
echo $transaction->amount;       // 600（已修改）

$bill = \App\Models\Document\Document::find(100);
echo $bill->status;              // partial（未重算）
echo $bill->paid;                // 500（因为关联交易金额已变，这里应该重新加载关系）

// 重新加载关系后
$bill->load('transactions');
echo $bill->paid;                // 600（动态计算正确，但 status 还是 partial）
```

---

### 实验三：API 文档专用更新成功（验证专用接口行为）

**前置条件**：同实验一

**操作步骤**：
1. 获取 API Token
2. 发送 PUT 请求到文档专用接口：
```http
PUT /api/documents/100/transactions/200
Content-Type: application/json
Authorization: Bearer {token}

{
    "amount": "600",
    "description": "API 文档专用修改测试"
}
```

**预期结果**：
- ✅ HTTP 201 Created，返回更新后的交易资源
- 交易金额变为 $600
- ✅ 账单状态**正确重算**（如果 $600 == $1000 则为 `paid`，否则为 `partial`）
- ✅ 账单 `paid` 动态属性返回 $600

**验证代码**：
```php
// 在 tinker 中验证
$transaction = \App\Models\Banking\Transaction::find(200);
echo $transaction->amount;       // 600（已修改）

$bill = \App\Models\Document\Document::find(100);
echo $bill->status;              // partial（因为 $600 < $1000）
echo $bill->paid;                // 600（正确）
```

---

### 实验四：API 通用更新带 document_id 被拦截（验证请求参数检查）

**前置条件**：同实验一

**操作步骤**：
1. 获取 API Token
2. 发送 PUT 请求（请求体**带** `document_id`）：
```http
PUT /api/transactions/200
Content-Type: application/json
Authorization: Bearer {token}

{
    "type": "income",
    "number": "TXN-200",
    "account_id": 1,
    "paid_at": "2026-05-20 00:00:00",
    "amount": "600",
    "currency_code": "USD",
    "currency_rate": "1",
    "document_id": 100,
    "category_id": 1,
    "payment_method": "offline-payment.cash"
}
```

**预期结果**：
- ❌ HTTP 400 Bad Request
- 返回错误：`transactions.messages.update_document_transaction_error`
- 交易金额仍为 $500

---

## 5. 权限口径与风险暴露差异分析

### 5.1 权限口径对比

| 维度 | Web 控制器 | API 通用控制器 | API 文档专用控制器 |
|------|-----------|---------------|-------------------|
| **权限严格度** | 🔒 最严格（交易已关联就拦截） | ⚠️ 宽松（只检查请求参数） | ✅ 设计合理（专用接口） |
| **保护对象** | 已关联的交易 | 防止通过通用接口建立关联 | 正确处理关联交易的更新 |
| **数据一致性保证** | ✅ 高（拦截修改，避免不一致） | ❌ 低（允许修改但不重算账单） | ✅ 高（正确重算账单状态） |
| **适用场景** | Web 界面手动编辑 | 通用 API 集成 | 账单/发票支付记录管理 |

### 5.2 风险暴露分析

#### 风险 1：API 通用接口可修改已关联交易
- **风险等级**：高
- **影响**：已关联账单的交易金额、日期等字段可以被修改，但账单状态不会同步更新
- **后果**：账单 `status` 与实际已付金额不一致，财务数据失真
- **示例**：账单 $1000，已匹配交易 $500（状态 `partial`），通过 API 将交易改为 $1000，账单状态仍为 `partial`，但 `paid` 动态计算为 $1000

#### 风险 2：请求参数检查语义错误
- **风险等级**：中
- **影响**：开发者可能误解 API 保护机制，以为已关联交易不能通过 API 修改
- **后果**：基于错误假设开发的集成代码可能导致数据不一致
- **问题根源**：`$request->has('document_id')` 检查的是"请求是否尝试设置关联"，而非"交易是否已关联"

#### 风险 3：Web 与 API 行为不一致
- **风险等级**：中
- **影响**：同样的操作在 Web 端被禁止，但在 API 端被允许
- **后果**：用户困惑，安全边界模糊，审计追踪困难

### 5.3 根本原因分析

1. **检查对象不一致**：
   - Web 控制器检查交易的**现有状态**
   - API 通用控制器检查请求的**传入参数**
   - 两者保护的是完全不同的语义

2. **Job 层授权不完整**：
   - `UpdateTransaction` Job 的 `authorize()` 只检查 `reconciled` 和 `isTransferTransaction()`
   - 缺少 `document_id` 检查，导致上层检查绕过

3. **缺少统一的授权层**：
   - 权限检查分散在控制器层，没有统一的策略
   - 不同控制器实现不同的检查逻辑，容易出现不一致

---

## 6. 修复建议

### 6.1 方案一：统一 Job 层授权（推荐）

在 `UpdateTransaction` Job 的 `authorize()` 方法中增加 `document_id` 检查：

```php
public function authorize(): void
{
    if ($this->model->reconciled) {
        $message = trans('messages.warning.reconciled_tran');
        throw new \Exception($message);
    }

    if ($this->model->isTransferTransaction()) {
        throw new \Exception(trans('messages.error.transfer_transaction'));
    }
    
    // 新增：检查是否已关联账单
    if ($this->model->document_id) {
        throw new \Exception(trans('transactions.messages.update_document_transaction'));
    }
}
```

**优点**：
- 从底层统一授权，所有调用 `UpdateTransaction` 的路径都会受到保护
- 无需修改多个控制器
- 符合"胖模型瘦控制器"的设计原则

**缺点**：
- 需要确保 `UpdateBankingDocumentTransaction` 不调用此 Job 的授权（目前它是嵌套调用，需要重构）

### 6.2 方案二：修复 API 通用控制器检查逻辑

修改 `Api\Banking\Transactions::update()` 检查交易状态而非请求参数：

```php
public function update(Transaction $transaction, Request $request)
{
    // 修复：检查交易本身是否已关联账单，而非请求参数
    if ($transaction->document_id) {
        return $this->errorBadRequest(trans('transactions.messages.update_document_transaction_error'));
    }

    $transaction = $this->dispatch(new UpdateTransaction($transaction, $request));

    return new Resource($transaction->fresh());
}
```

**优点**：
- 最小改动，风险低
- 与 Web 控制器行为一致

**缺点**：
- 仍依赖控制器层检查，Job 层仍然脆弱

### 6.3 方案三：双重检查（最安全）

同时修复 Job 层和 API 控制器层，确保多层防护。

---

## 7. 总结

### 7.1 验证结论

| 路径 | 请求不带 document_id 时是否允许更新 | 账单状态是否重算 |
|------|-----------------------------------|-----------------|
| Web 控制器 | ❌ 不允许 | - |
| API 通用控制器 | ✅ **允许（漏洞！）** | ❌ 不重算（数据不一致） |
| API 文档专用控制器 | ✅ 允许（设计如此） | ✅ 正确重算 |

### 7.2 核心发现

1. **API 通用控制器存在权限漏洞**：已关联账单的未对账交易可以通过 API 通用接口修改，且不会触发账单状态重算
2. **检查语义错误**：`$request->has('document_id')` 只防止"设置关联"，不保护"已有交易"
3. **三层口径不一致**：Web/API-通用/API-专用 三条路径对 `document_id` 的检查逻辑完全不同
4. **Job 层授权不完整**：`UpdateTransaction` Job 缺少 `document_id` 检查，是风险的根源

### 7.3 关键代码引用

| 问题 | 代码位置 |
|------|---------|
| Web 控制器正确检查 | `app/Http/Controllers/Banking/Transactions.php:250` |
| API 通用控制器错误检查 | `app/Http/Controllers/Api/Banking/Transactions.php:64` |
| Job 层缺少检查 | `app/Jobs/Banking/UpdateTransaction.php:65-76` |
| API 专用控制器正确处理 | `app/Http/Controllers/Api/Document/DocumentTransactions.php:83-92` |
| 专用 Job 正确重算 | `app/Jobs/Banking/UpdateBankingDocumentTransaction.php:92-115` |
