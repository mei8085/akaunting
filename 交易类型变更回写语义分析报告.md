# Akaunting 交易类型变更后的回写语义分析报告

> **核心问题**：已关联账单的交易在 API 通用更新中修改 `type` 后，删除阶段的 Observer 会如何按 type 选择 invoice/bill 并回写 sent/received 基线，是否会造成状态语义错位。

---

## 1. 核心代码结构分析

### 1.1 Transaction 模型的三重关系定义

**文件**: `app/Models/Banking/Transaction.php:111-144`

```php
/**
 * 三个关系方法底层完全相同！
 */
public function bill()
{
    return $this->belongsTo('App\Models\Document\Document', 'document_id')
        ->withoutGlobalScope('App\Scopes\Document');
}

public function invoice()
{
    return $this->belongsTo('App\Models\Document\Document', 'document_id')
        ->withoutGlobalScope('App\Scopes\Document');
}

public function document()
{
    return $this->belongsTo('App\Models\Document\Document', 'document_id')
        ->withoutGlobalScope('App\Scopes\Document');
}
```

**关键发现**：
- `bill()`、`invoice()`、`document()` 三个关系方法**底层逻辑完全相同**
- 都通过同一个外键 `document_id` 关联到 `Document` 模型
- 都使用了 `withoutGlobalScope` 移除类型过滤
- **唯一区别只是方法名不同**，没有按文档类型进行隐式过滤

> 这意味着：无论调用 `$transaction->invoice` 还是 `$transaction->bill`，只要 `document_id` 相同，返回的是同一个 Document 对象。

### 1.2 Observer 的类型判断逻辑

**文件**: `app/Observers/Transaction.php:23-29`

```php
public function deleted(Model $transaction)
{
    if (! empty($transaction->document_id)) {
        // 🔴 关键：根据交易当前的 type 决定文档类型
        $type = ($transaction->type == 'income') ? Document::INVOICE_TYPE : Document::BILL_TYPE;

        $this->updateDocument($transaction, $type);
    }
}
```

### 1.3 状态回写逻辑

**文件**: `app/Observers/Transaction.php:36-59`

```php
protected function updateDocument($transaction, $type)
{
    // 🔴 动态调用关系方法：invoice() 或 bill()
    // 但因为关系定义相同，实际查询到的是同一个文档
    $document = $transaction->{$type};

    if (empty($document)) {
        return;
    }

    $document->transactions_count = $document->transactions->count();

    event(new TransactionsCounted($document));

    // 🔴 关键：根据 $type 变量决定基线状态
    // invoice → sent, bill → received
    $document->status = ($type == Document::INVOICE_TYPE) ? 'sent' : 'received';

    if ($document->transactions_count > 0) {
        $document->status = 'partial';
    }

    unset($document->transactions_count);

    $document->save();
}
```

### 1.4 文档类型常量定义

**文件**: `app/Models/Document/Document.php:26-29`

```php
public const INVOICE_TYPE = 'invoice';
public const INVOICE_RECURRING_TYPE = 'invoice-recurring';
public const BILL_TYPE = 'bill';
public const BILL_RECURRING_TYPE = 'bill-recurring';
```

---

## 2. API 通用更新中 type 修改的完整路径

### 2.1 调用链路

```
PUT /api/transactions/{id}
    ↓
Api\Banking\Transactions::update()
    ↓（检查 $request->has('document_id')，不检查交易现有状态）
UpdateTransaction Job
    ↓（Job authorize 只检查 reconciled 和 transfer，不检查 document_id）
$this->model->update($this->request->all())
    ↓
Transaction 模型的 update() 方法
    ↓
交易 type 被修改，document_id 保持不变
```

### 2.2 关键代码路径

**API 控制器** (`app/Http/Controllers/Api/Banking/Transactions.php:62-71`):
```php
public function update(Transaction $transaction, Request $request)
{
    // 只检查请求体中是否包含 document_id，不检查交易是否已关联
    if ($request->has('document_id')) {
        return $this->errorBadRequest(...);
    }

    // 直接进入 Job，允许修改 type
    $transaction = $this->dispatch(new UpdateTransaction($transaction, $request));

    return new Resource($transaction->fresh());
}
```

**Job 类型标准化** (`app/Jobs/Banking/UpdateTransaction.php:20-29`):
```php
// 如果传入的 type 不在配置中，自动标准化为 income 或 expense
if (! array_key_exists($this->request->get('type'), config('type.transaction'))) {
    $isExpense = str_contains((string) $this->request->get('type', ''), 'expense');
    // ...
    $type = $isExpense ? Transaction::EXPENSE_TYPE : Transaction::INCOME_TYPE;
    $this->request->merge(['type' => $type]);
}
```

**最终更新** (`app/Jobs/Banking/UpdateTransaction.php:32`):
```php
$this->model->update($this->request->all());
```

> **结论**：API 通用更新允许修改已关联账单交易的 `type` 字段，且不会触发任何账单状态重算。

---

## 3. 状态语义错位的完整场景分析

### 3.1 错位场景复现

| 步骤 | 操作 | 交易状态 | 文档状态 | 关键值 |
|------|------|---------|---------|--------|
| 1 | 创建发票（invoice） | - | `sent` | 文档 ID=100，type=invoice，金额=$1000 |
| 2 | 创建收入交易并匹配 | type=income，document_id=100 | `partial` | 交易 ID=200，amount=$500 |
| 3 | 通过 API 修改交易 type 为 expense | type=expense，document_id=100 | `partial`（未重算） | 交易 type 已改变，关联保持 |
| 4 | 删除该交易 | （交易删除） | ❓ | Observer 触发 deleted 事件 |

### 3.2 删除时的执行流程

**Step 1**: Observer 读取交易当前 type
```php
$transaction->type;  // 'expense'（已被修改）
```

**Step 2**: 决定文档类型
```php
$type = ($transaction->type == 'income') ? Document::INVOICE_TYPE : Document::BILL_TYPE;
// $type = 'bill' （🔴 错误！文档实际是 invoice 类型）
```

**Step 3**: 动态获取文档
```php
$document = $transaction->{$type};  // $transaction->bill()
// 虽然方法名是 bill，但因为关系定义相同
// 实际查询：SELECT * FROM documents WHERE id = 100
// 返回的文档实际类型是 invoice，不是 bill
```

**Step 4**: 决定基线状态
```php
$document->status = ($type == Document::INVOICE_TYPE) ? 'sent' : 'received';
// $type = 'bill'，所以 $document->status = 'received'
// 🔴 错误！invoice 类型文档的未支付基线应该是 'sent'，不是 'received'
```

**Step 5**: 根据交易条数调整状态
```php
if ($document->transactions_count > 0) {
    $document->status = 'partial';
}
// 假设这是唯一关联的交易，count = 0
// 所以状态保持为 'received'
```

**Step 6**: 保存文档
```php
$document->save();
// ❌ invoice 类型文档的 status 被错误地设置为 'received'
```

### 3.3 语义错位的本质

| 维度 | 期望值（基于文档真实类型） | 实际值（基于交易当前 type） | 说明 |
|------|--------------------------|--------------------------|------|
| 文档真实类型 | `invoice` | - | Document.type = 'invoice' |
| 交易当前类型 | - | `expense` | Transaction.type 已被修改 |
| Observer 判断的文档类型 | `invoice` | `bill` | Observer 用交易 type 推导 |
| 基线状态 | `sent`（invoice 的未支付状态） | `received`（bill 的未支付状态） | 语义完全错位 |

> **核心问题**：Observer 依赖**交易的 type**来推断文档类型和基线状态，而不是依赖**文档自身的 type 字段**。当交易 type 被修改后，这个推断就完全错误了。

---

## 4. 触发条件矩阵

### 4.1 所有可能的组合场景

| 原始交易 type | 关联文档真实 type | 修改后的交易 type | 删除时 Observer 判断的文档 type | 应设置的基线状态 | 实际设置的基线状态 | 语义是否错位 | 错位程度 |
|-------------|-----------------|-----------------|-----------------------------|----------------|----------------|-------------|---------|
| `income` | `invoice` | `income`（未修改） | `invoice` ✅ | `sent` | `sent` ✅ | 否 | - |
| `income` | `invoice` | `expense` | `bill` ❌ | `sent` | `received` ❌ | 是 | 严重 |
| `expense` | `bill` | `expense`（未修改） | `bill` ✅ | `received` | `received` ✅ | 否 | - |
| `expense` | `bill` | `income` | `invoice` ❌ | `received` | `sent` ❌ | 是 | 严重 |
| `income` | `bill`（异常关联） | `income` | `invoice` ❌ | `received` | `sent` ❌ | 是 | 严重 |
| `expense` | `invoice`（异常关联） | `expense` | `bill` ❌ | `sent` | `received` ❌ | 是 | 严重 |

### 4.2 错位风险等级

| 场景 | 触发概率 | 影响范围 | 风险等级 |
|------|---------|---------|---------|
| 正常未修改 type | 高 | 无 | ✅ 安全 |
| API 修改 type 后删除 | 中（API 漏洞存在） | 单个文档状态错误 | 🔴 高 |
| 异常关联（交易与文档类型不匹配） | 低（正常流程不会出现） | 单个文档状态错误 | 🟡 中 |

---

## 5. 最小可复现实验步骤

### 实验：income 改 expense 后删除导致状态错位

**前置条件**：
- 系统已安装，API Token 已获取
- 默认账户、分类、支付方式已配置

**操作步骤**：

#### Step 1: 创建发票（Invoice）
```http
POST /api/documents
Content-Type: application/json
Authorization: Bearer {token}

{
    "type": "invoice",
    "document_number": "INV-001",
    "status": "sent",
    "issued_at": "2026-05-20",
    "due_at": "2026-06-20",
    "amount": "1000.00",
    "currency_code": "USD",
    "currency_rate": "1",
    "contact_id": 1,
    "contact_name": "Test Customer",
    "contact_email": "test@example.com",
    "items": [
        {
            "name": "Service",
            "quantity": "1",
            "price": "1000.00"
        }
    ],
    "category_id": 1
}
```
**记录返回的文档 ID**（假设为 100），此时状态为 `sent`。

#### Step 2: 创建收入交易并关联发票
```http
POST /api/documents/100/transactions
Content-Type: application/json
Authorization: Bearer {token}

{
    "amount": "500.00",
    "account_id": 1,
    "paid_at": "2026-05-20 00:00:00",
    "description": "Partial payment"
}
```
**记录返回的交易 ID**（假设为 200）。
**验证**：
- 交易 `type = 'income'`，`document_id = 100`
- 发票状态变为 `partial`

#### Step 3: 通过 API 通用接口修改交易 type 为 expense
```http
PUT /api/transactions/200
Content-Type: application/json
Authorization: Bearer {token}

{
    "type": "expense",
    "number": "TXN-200",
    "account_id": 1,
    "paid_at": "2026-05-20 00:00:00",
    "amount": "500.00",
    "currency_code": "USD",
    "currency_rate": "1",
    "category_id": 1,
    "payment_method": "offline-payment.cash"
}
```
**注意**：请求体中**不要**包含 `document_id` 字段。

**验证**：
- 交易 `type` 已变为 `'expense'`
- 交易 `document_id` 仍然是 100（关联保持）
- 发票状态**仍然**是 `partial`（未重算，符合预期）

#### Step 4: 删除该交易
```http
DELETE /api/transactions/200
Authorization: Bearer {token}
```

#### Step 5: 验证状态错位

**在 tinker 中验证**：
```php
// 验证交易已删除
$transaction = \App\Models\Banking\Transaction::find(200);
echo $transaction ? '存在' : '已删除';  // 应输出 '已删除'

// 验证发票状态
$invoice = \App\Models\Document\Document::find(100);
echo $invoice->type;    // 应输出 'invoice'（文档真实类型）
echo $invoice->status;  // ❌ 输出 'received'，而不是预期的 'sent'
```

**预期结果（错误）**：
- 发票类型：`invoice` ✅
- 发票状态：`received` ❌（应为 `sent`）

---

## 6. 语义偏差的深度分析

### 6.1 sent 与 received 的语义边界

| 状态 | 适用文档类型 | 语义 |
|------|-------------|------|
| `sent` | Invoice（发票） | 已发送给客户，等待付款 |
| `received` | Bill（账单） | 已收到供应商账单，等待付款 |

**语义错位的影响**：
1. **业务含义错误**：发票被标记为 `received`（已收到），但实际上发票是"已发送"给客户的
2. **列表显示错误**：在发票列表中筛选 `sent` 状态时，该发票不会出现
3. **统计报表错误**：基于状态的财务统计数据失真
4. **工作流中断**：依赖状态触发的后续业务流程（如催款）不会执行

### 6.2 与金额语义的偏差分析

状态回写策略还存在另一层偏差：**基于交易条数而非金额**。

| 场景 | 交易条数 | 实际已付金额 | 基于条数的状态 | 基于金额的预期状态 | 偏差 |
|------|---------|------------|--------------|------------------|------|
| 删除后无关联交易 | 0 | $0 | `sent`/`received` | `sent`/`received` | 无 |
| 剩余1条 $0 金额交易 | 1 | $0 | ❌ `partial` | `sent`/`received` | 有 |
| 剩余2条总金额 = 账单金额 | 2 | $1000 | ❌ `partial` | `paid` | 有 |

**双重偏差**：
1. **类型选择偏差**：用交易 type 而非文档 type 决定基线状态
2. **金额语义偏差**：用交易条数而非实际金额决定状态

---

## 7. 根本原因与修复建议

### 7.1 根本原因

1. **关系定义冗余且误导**：`invoice()` 和 `bill()` 方法名暗示了类型过滤，但实际没有
2. **类型推断来源错误**：Observer 依赖交易的 type 推断文档类型，而不是读取文档自身的 type 字段
3. **API 权限漏洞**：API 通用更新允许修改已关联交易的 type，且不触发状态重算
4. **缺少一致性校验**：没有机制确保交易 type 与关联文档 type 的一致性

### 7.2 修复方案

#### 方案一：修复 Observer 的类型推断逻辑（推荐，最小改动）

```php
// 修改 app/Observers/Transaction.php:26
protected function updateDocument($transaction, $type)
{
    // 🔴 原代码（错误）
    // $document = $transaction->{$type};
    
    // ✅ 修复：直接使用 document() 关系，不依赖方法名
    $document = $transaction->document;
    
    if (empty($document)) {
        return;
    }
    
    $document->transactions_count = $document->transactions->count();
    
    event(new TransactionsCounted($document));
    
    // 🔴 原代码（错误）
    // $document->status = ($type == Document::INVOICE_TYPE) ? 'sent' : 'received';
    
    // ✅ 修复：根据文档自身的 type 决定基线状态
    $document->status = ($document->type == Document::INVOICE_TYPE) ? 'sent' : 'received';
    
    // ... 其余逻辑保持不变
}
```

**优点**：
- 最小改动，只修改 Observer
- 不依赖交易 type，直接读取文档真实类型
- 无论交易 type 是否被修改，都能正确推断

#### 方案二：增加 API 层的 type 修改限制

在 API 通用更新中，如果交易已关联账单，禁止修改 type：

```php
// 修改 app/Http/Controllers/Api/Banking/Transactions.php:62
public function update(Transaction $transaction, Request $request)
{
    if ($request->has('document_id')) {
        return $this->errorBadRequest(...);
    }
    
    // ✅ 新增：如果交易已关联账单，禁止修改 type
    if ($transaction->document_id && $request->has('type') && $request->get('type') != $transaction->type) {
        return $this->errorBadRequest(trans('transactions.messages.update_type_document_transaction'));
    }

    $transaction = $this->dispatch(new UpdateTransaction($transaction, $request));

    return new Resource($transaction->fresh());
}
```

#### 方案三：双重修复（最安全）

同时修复 Observer 和 API 层，确保多层防护。

### 7.3 额外的一致性校验

建议在匹配交易时增加一致性校验：

```php
// 在 MatchBankingDocumentTransaction 中增加
public function authorize(): void
{
    parent::authorize();
    
    // 确保交易类型与文档类型匹配
    $expected_transaction_type = $this->model->type == Document::INVOICE_TYPE 
        ? Transaction::INCOME_TYPE 
        : Transaction::EXPENSE_TYPE;
    
    if ($this->transaction->type != $expected_transaction_type) {
        throw new \Exception('Transaction type does not match document type');
    }
}
```

---

## 8. 总结

### 8.1 核心发现

1. **关系方法名误导**：`invoice()` 和 `bill()` 方法名暗示类型过滤，但实际没有，容易产生误解
2. **类型推断来源错误**：Observer 用**交易的 type**推断文档类型，而不是**文档自身的 type**
3. **状态语义错位**：income 改 expense 后删除，invoice 会被错误标记为 `received` 而非 `sent`
4. **双重语义偏差**：同时存在"类型选择偏差"和"金额语义偏差"（条数 vs 金额）
5. **API 漏洞叠加**：API 通用更新允许修改已关联交易的 type，为错位创造了条件

### 8.2 关键代码引用

| 问题 | 代码位置 |
|------|---------|
| 关系定义冗余 | `app/Models/Banking/Transaction.php:111-144` |
| Observer 类型推断 | `app/Observers/Transaction.php:26` |
| Observer 状态回写 | `app/Observers/Transaction.php:48` |
| API 类型修改漏洞 | `app/Http/Controllers/Api/Banking/Transactions.php:62-71` |
| Job 无 document_id 检查 | `app/Jobs/Banking/UpdateTransaction.php:65-76` |

### 8.3 可验证边界案例

| 案例 | 触发条件 | 预期结果 | 实际结果 | 错位类型 |
|------|---------|---------|---------|---------|
| income → expense 后删除 | API 修改 type，交易关联 invoice | invoice 状态 = `sent` | invoice 状态 = `received` | 类型选择错位 |
| expense → income 后删除 | API 修改 type，交易关联 bill | bill 状态 = `received` | bill 状态 = `sent` | 类型选择错位 |
| 删除后剩余零金额交易 | 存在 $0 金额的关联交易 | 状态 = `sent`/`received` | 状态 = `partial` | 金额语义错位 |
| 删除后剩余多条总金额付清 | 多条交易总金额 = 账单金额 | 状态 = `paid` | 状态 = `partial` | 金额语义错位 |
