# 批量操作与授权检查配合机制分析报告

## 一、整体架构概览

Akaunting 系统对批量操作的权限校验采用了**「三层防护、整体授权、逐条业务校验、部分成功容忍」**的混合策略。权限校验分三个阶段执行：

| 阶段 | 位置 | 校验类型 | 粒度 | 失败后果 |
|------|------|----------|------|----------|
| 第一层 | 视图组件渲染 | 用户权限 | 操作类型 | 无权按钮不显示 |
| 第二层 | 控制器入口 | 用户权限 | 操作类型 | 整批拒绝，403返回 |
| 第三层 | Job 执行时 | 业务规则 | 单条记录 | 单条跳过，记录错误，其余继续 |

---

## 二、第一层：前端 UI 层过滤（按钮可见性）

**代码位置**：[Bulkaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/View/Components/Index/Bulkaction.php#L42-L107)

在 `getActions()` 方法中，遍历批量操作配置，根据权限过滤操作按钮：

```php
foreach ($bulk_action->actions as $key => $action) {
    if ((isset($action['permission']) && ! user()->can($action['permission']))) {
        continue;  // 无权则跳过，按钮不渲染
    }
    $actions[$key] = $action;
}
```

**作用**：防止无权限用户在界面上看到操作按钮，属于体验层面的第一道防线。

---

## 三、第二层：控制器入口 — **整体权限校验**

**代码位置**：[BulkActions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Http/Controllers/Common/BulkActions.php#L21-L125)

### 3.1 路由注册

路由在 [admin.php:42](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/routes/admin.php#L42-L42)：
```php
Route::post('bulk-actions/{group}/{type}', 'Common\BulkActions@action')->name('bulk-actions.action');
```

### 3.2 整体权限校验（关键逻辑）

在 `action()` 方法 **第 50-63 行**：

```php
if (
    isset($bulk_actions->actions[$handle]['permission'])
    && ! user()->can($bulk_actions->actions[$handle]['permission'])
) {
    flash(trans('errors.message.403'))->error()->important();

    return response()->json([
        'success' => false,
        'redirect' => true,
        'error' => true,
        'data' => [],
        'message' => trans('errors.message.403')
    ]);
}
```

**校验结论**：
- ✅ **权限校验是「整体」进行的**，不是逐条
- 检查的是当前用户是否拥有该操作类型对应的权限（如 `delete-sales-invoices`）
- 如果用户无此权限，**整批操作立即终止**，返回 403 错误，不会进入逐条处理阶段
- 不关心选中记录的个体差异，只判断用户对「该类资源的该类操作」是否有权限

### 3.3 与普通控制器的权限校验对比

普通单条操作（如删除一条发票）通过 **Controller 基类中间件** 实现：

**代码位置**：[Permissions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Traits/Permissions.php#L425-L500)

```php
// assignPermissionsToController() 中：
$this->middleware('permission:delete-' . $controller)->only('destroy');
```

如 [Invoices::destroy()](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Http/Controllers/Sales/Invoices.php#L223-L240) 方法会被 `permission:delete-sales-invoices` 中间件保护。

而 `BulkActions` 控制器**不走这个中间件自动映射**，而是在方法内手工检查 `$bulk_actions->actions[$handle]['permission']`。这是两者的关键区别。

---

## 四、第三层：逐条执行 — **业务规则校验（非权限）**

### 4.1 批量操作基类中的逐条循环模式

**代码位置**：[BulkAction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Abstracts/BulkAction.php#L152-L260)

所有批量操作都遵循 **「foreach + try/catch + dispatch Job」** 的模式：

```php
public function deleteContacts($request)
{
    $contacts = $this->getSelectedRecords($request, 'user');

    foreach ($contacts as $contact) {
        try {
            $this->dispatch(new DeleteContact($contact));  // 分发 Job
        } catch (\Exception $e) {
            flash($e->getMessage())->error()->important();  // 单条失败不终止
        }
    }
}
```

此模式出现在：
- [BulkAction.php::deleteContacts()](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Abstracts/BulkAction.php#L236-L247)
- [BulkAction.php::disableContacts()](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Abstracts/BulkAction.php#L223-L234)
- [BulkAction.php::deleteTransactions()](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Abstracts/BulkAction.php#L249-L260)
- [Invoices.php::update()](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/BulkActions/Sales/Invoices.php#L74-L100)
- [Invoices.php::destroy()](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/BulkActions/Sales/Invoices.php#L139-L152)
- [Customers.php::update()](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/BulkActions/Sales/Customers.php#L63-L79)
- [Items.php::destroy()](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/BulkActions/Common/Items.php#L81-L92)
- ...（所有 BulkActions 子类）

### 4.2 Job 中的 authorize() — 是业务规则校验，不是用户权限

每个 Job 在 `handle()` 开头调用 `$this->authorize()`，但这个 authorize() **检查的是业务约束，而非用户 ACL 权限**。举例：

| Job | authorize() 检查内容 | 业务含义 |
|-----|---------------------|----------|
| [DeleteDocument](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Jobs/Document/DeleteDocument.php#L42-L50) | `$this->model->transactions()->isReconciled()->count()` | **已对账的单据不能删除** |
| [UpdateDocument](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Jobs/Document/UpdateDocument.php#L92-L112) | 更换往来单位时检查状态是否锁定、是否已对账 | **锁定状态/已对账不能改往来单位** |
| [DeleteContact](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Jobs/Common/DeleteContact.php#L40-L47) | `$this->getRelationships()` 检查关联交易/单据 | **有关联数据的客户/供应商不能删除** |
| [UpdateContact](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Jobs/Common/UpdateContact.php#L59-L66) | 禁用时检查关联交易/单据 | **有关联数据的客户/供应商不能禁用** |

示例代码（DeleteDocument 的 authorize）：
```php
public function authorize(): void
{
    if ($this->model->transactions()->isReconciled()->count()) {
        $type = Str::plural($this->model->type);
        $message = trans('messages.warning.reconciled_doc', [...]);
        throw new \Exception($message);
    }
}
```

### 4.3 没有逐条做用户权限校验

**重要发现**：在整个 BulkActions 流程中，**不存在逐条检查「用户对该条记录是否有权限」**的代码。即：

```
❌ 没有类似 foreach($items as $item) { if(!user()->can('delete', $item)) { skip; } } 的逻辑
❌ 没有使用 Laravel Policy 的 authorize($model, $action)
```

**原因**：系统的权限模型是**「资源类型级」**的（如 `delete-sales-invoices`），不是**「记录级」**的（Row-level security）。用户要么能删所有发票，要么一张都不能删，不存在能删 A 发票但不能删 B 发票的权限设计。

---

## 五、部分记录无权（业务校验失败）时的整批处理策略

### 5.1 错误计数机制

**代码位置**：[BulkActions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Http/Controllers/Common/BulkActions.php#L67-L93)

```php
$result = $bulk_actions->{$handle}($request);  // 执行批量操作

$count = count($request->get('selected'));     // 选中的总数
$not_passed = 0;                                // 失败计数器

// 通过统计 flash 消息中的「danger/warning」级别来推断失败条数
flash()->messages->each(function ($message) use (&$not_passed) {
    if (in_array($message->level, ['danger', 'warning'])) {
        $not_passed++;
    }
});
```

**设计缺陷提示**：这种通过 flash 消息数量反推失败条数的方式**不够精确**——如果某条记录产生多条 warning 消息，或非单条记录相关的错误被计入，会导致统计失真。

### 5.2 返回消息的策略

```php
// 默认消息模板：显示「成功处理了 N 条」
$message = trans($bulk_actions->messages['general'], [
    'type' => $handle, 
    'count' => $count - $not_passed  // 实际成功数 = 总数 - 失败数
]);

// 如果全部成功且有专用消息，用专用消息
if (array_key_exists($handle, $bulk_actions->messages) && $not_passed === 0) {
    $message = trans($bulk_actions->messages[$handle], ['type' => $page]);
}

// 有失败时 flash 级别降为 info，全部成功为 success
$level = $not_passed > 0 ? 'info' : 'success';
```

### 5.3 策略总结

| 场景 | 处理方式 |
|------|----------|
| 用户无该操作类型权限 | **整批拒绝**，返回 403，0 条处理 |
| 所有记录都通过业务校验 | **全部成功**，显示 success 消息 |
| N 条记录中 K 条业务校验失败 | **部分成功**，成功 N-K 条，失败 K 条逐条记录错误，显示 info 级消息「成功处理了 N-K 条」 |
| 单条记录业务校验异常 | **try/catch 捕获**，不回滚已处理的记录，继续处理下一条 |

---

## 六、完整调用链路图

```
用户勾选记录 → 点击批量删除按钮
        │
        ▼
  [前端 Vue 组件] 提交 selected IDs + handle=delete
        │
        ▼
  [POST bulk-actions/sales/invoices]
        │
        ▼
  BulkActionsController@action
        │
        ├─→ 实例化 Sales\Invoices BulkAction 类
        │
        ├─→ 【第二层：整体权限校验】
        │     user()->can('delete-sales-invoices') ?
        │          ├─ 否 → 返回 403 JSON，流程结束
        │          └─ 是 → 继续
        │
        ├─→ $bulk_actions->destroy($request)  ← 进入具体操作
        │        │
        │        └─→ foreach ($invoices as $invoice) {
        │              │
        │              ├─→ 【第三层：逐条业务校验】
        │              │     DeleteDocument Job::handle()
        │              │       └─→ $this->authorize()
        │              │            ├─ 检查是否已对账
        │              │            │    ├─ 已对账 → throw Exception
        │              │            │    │           → catch → flash(error)
        │              │            │    │           → 继续下一条 ✓
        │              │            │    └─ 未对账 → 执行删除 ✓
        │              │            └─ 无用户权限检查（第二层已保证）
        │              │
        │              └─→ 下一条记录
        │            }
        │
        ├─→ 统计 $not_passed（通过 flash 消息数量）
        │
        └─→ 返回 JSON：
              success: true
              message: "成功处理了 {count-not_passed} 条"
              level:   not_passed>0 ? "info" : "success"
```

---

## 七、关键结论

### 7.1 核心回答

| 问题 | 答案 |
|------|------|
| 权限校验是逐条还是整体？ | **用户 ACL 权限是「整体」校验的**（第二层）；逐条做的是「业务规则校验」，不是权限（第三层） |
| 部分记录无权时整批怎么处理？ | **用户权限层面**无部分无权的概念——要么全部可操作，要么整批拒绝；**业务规则层面**部分失败时，成功的记录保留，失败的记录跳过并记录错误，最终消息显示实际成功条数 |

### 7.2 设计特点

1. **权限模型简单**：只有资源类型级权限（CRUD + 资源组），无记录级权限，因此无需逐条检查用户权限
2. **两阶段校验分离**：用户权限（入口一次性） vs 业务规则（逐条），职责清晰
3. **部分成功容忍**：不使用数据库事务包裹整批操作，允许「尽可能多处理」，通过错误消息反馈失败项
4. **失败计数间接**：通过 flash 消息数量推断失败数，非严谨设计，但简单够用

### 7.3 代码索引

| 关注点 | 文件 | 关键行 |
|--------|------|--------|
| 批量操作总控制器 | [BulkActions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Http/Controllers/Common/BulkActions.php) | L50-L63（整体权限）、L67-L93（结果汇总） |
| 批量操作抽象基类 | [BulkAction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Abstracts/BulkAction.php) | L25-L51（actions 配置含 permission）、L168-L260（各类 foreach 循环） |
| 请求验证类 | [BulkAction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Http/Requests/Common/BulkAction.php) | L14-L20（仅验证 handle+selected 必填） |
| 前端按钮权限过滤 | [Bulkaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/View/Components/Index/Bulkaction.php) | L92-L104（权限过滤循环） |
| 权限中间件映射 | [Permissions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Traits/Permissions.php) | L425-L500（普通控制器 assignPermissionsToController） |
| 发票批量操作示例 | [Invoices.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/BulkActions/Sales/Invoices.php) | L26-L65（permission 配置）、L139-L152（destroy + try/catch） |
| 删除发票 Job 业务校验 | [DeleteDocument.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Jobs/Document/DeleteDocument.php) | L42-L50（authorize：已对账不可删） |
| 删除客户 Job 业务校验 | [DeleteContact.php](file:///d:/fz/0601-1/solo-dogfeeding/code/70-akaunting/app/Jobs/Common/DeleteContact.php) | L40-L47（authorize：有关联数据不可删） |
