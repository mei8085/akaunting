# Akaunting 模块菜单注入与权限边界分析报告

## 目录

1. [概述](#概述)
2. [菜单注入机制](#菜单注入机制)
3. [权限边界定义](#权限边界定义)
4. [模块启用流程](#模块启用流程)
5. [模块开发规范](#模块开发规范)
6. [总结](#总结)

---

## 概述

Akaunting 采用基于事件驱动的模块化架构，使用 `akaunting/laravel-module` 作为模块管理基础，`akaunting/laravel-menu` 作为菜单管理库，`santigarcor/laratrust` 作为权限管理系统。模块通过监听菜单构建事件来注入自己的菜单项，并通过标准化的权限命名约定定义访问控制边界。

---

## 菜单注入机制

### 1. 菜单构建生命周期

菜单的构建发生在 HTTP 请求的中间件阶段，采用**事件驱动**的方式进行：

| 阶段 | 事件 | 说明 |
|------|------|------|
| 创建前 | `AdminCreating` | 菜单对象已创建，尚未添加任何项目 |
| 创建后 | `AdminCreated` | 核心菜单已添加，模块可在此阶段注入 |

**核心触发点**：[AdminMenu.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Http/Middleware/AdminMenu.php#L25-L31)

```php
menu()->create('admin', function ($menu) {
    $menu->style('tailwind');

    event(new AdminCreating($menu));

    event(new AdminCreated($menu));
});
```

### 2. 中间件注册

菜单中间件在 `admin` 路由组中被注册，确保每个后台请求都会构建菜单：

**注册位置**：[Kernel.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Http/Kernel.php#L76-L88)

```php
'admin' => [
    'web',
    'auth',
    // ...
    'menu.admin',           // 菜单构建中间件
    'permission:read-admin-panel',
    // ...
],
```

### 3. 核心菜单注入

系统核心菜单由 `ShowInAdmin` 监听器负责添加，它监听 `AdminCreated` 事件：

**核心监听器**：[ShowInAdmin.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Listeners/Menu/ShowInAdmin.php)

核心菜单项包括：
- 仪表盘 (Dashboards)
- 商品 (Items)
- 销售 (Sales) - 包含发票、客户子菜单
- 采购 (Purchases) - 包含账单、供应商子菜单
- 银行 (Banking) - 包含账户、交易、转账、对账子菜单
- 报表 (Reports)
- 应用 (Apps/Modules)

### 4. 模块菜单注入方式

模块通过以下步骤向主界面注入菜单：

#### 4.1 事件服务提供者

每个模块都有自己的 `Event` 服务提供者，启用事件自动发现：

**模板文件**：[event.stub](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Console/Stubs/Modules/providers/event.stub)

```php
class Event extends Provider
{
    public function shouldDiscoverEvents()
    {
        return true;
    }

    protected function discoverEventsWithin()
    {
        return [
            __DIR__ . '/../Listeners',
        ];
    }
}
```

#### 4.2 菜单监听器

模块在 `Listeners/Menu/` 目录下创建菜单监听器，监听菜单构建事件：

```php
// Modules/Blog/Listeners/Menu/ShowInAdmin.php
namespace Modules\Blog\Listeners\Menu;

use App\Events\Menu\AdminCreated as Event;
use App\Traits\Permissions;

class ShowInAdmin
{
    use Permissions;

    public $alias = 'blog';

    public function handle(Event $event)
    {
        $menu = $event->menu;

        $title = trim(trans('blog::general.name'));
        if ($this->canAccessMenuItem($title, 'read-blog-posts')) {
            $menu->route('blog.posts.index', $title, [], 55, ['icon' => 'article']);
        }
    }
}
```

### 5. 菜单类型

系统支持多种菜单类型，每种类型对应不同的事件：

| 菜单类型 | 创建事件 | 完成事件 | 监听器示例 |
|---------|---------|---------|-----------|
| 管理后台菜单 | `AdminCreating` | `AdminCreated` | `ShowInAdmin` |
| 门户菜单 | `PortalCreating` | `PortalCreated` | `ShowInPortal` |
| 设置菜单 | `SettingsCreating` | `SettingsCreated` | `ShowInSettings` |
| 个人资料菜单 | `ProfileCreating` | `ProfileCreated` | `ShowInProfile` |
| 新建菜单 | `NewwCreating` | `NewwCreated` | `ShowInNeww` |
| 通知菜单 | `NotificationsCreating` | `NotificationsCreated` | `ShowInNotifications` |

**设置菜单示例**：[ShowInSettings.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Listeners/Menu/ShowInSettings.php)

### 6. 菜单呈现器

菜单的 HTML 渲染由 `Menu` 呈现器负责，使用 Tailwind CSS 样式：

**呈现器类**：[Menu.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/View/Presenters/Menu.php)

支持的菜单样式：
- 普通菜单项 (`getMenuWithoutDropdownWrapper`)
- 下拉菜单项 (`getMenuWithDropDownWrapper`)
- 多级下拉菜单 (`getMultiLevelDropdownWrapper`)
- 分隔线 (`getDividerWrapper`)
- 标题 (`getHeaderWrapper`)

---

## 权限边界定义

### 1. 权限系统架构

Akaunting 使用 **Laratrust** (santigarcor/laratrust) 作为 RBAC (基于角色的访问控制) 系统：

**配置文件**：[laratrust.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/config/laratrust.php)

**核心模型**：
- [Permission.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Models/Auth/Permission.php) - 权限模型
- `Role` - 角色模型
- `User` - 用户模型 (通过 Laratrust 扩展)

### 2. 权限命名约定

权限名称遵循统一的命名格式：

```
{action}-{module/area}-{resource}
```

**示例**：

| 权限名称 | 说明 |
|---------|------|
| `read-sales-invoices` | 读取销售发票 |
| `create-purchases-bills` | 创建采购账单 |
| `update-banking-accounts` | 更新银行账户 |
| `delete-common-items` | 删除通用项目 |
| `read-blog-posts` | 读取博客文章 (模块权限) |

**动作缩写映射**（定义于 [Permissions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Traits/Permissions.php#L18-L26)）：

| 缩写 | 完整动作 |
|------|---------|
| c | create |
| r | read |
| u | update |
| d | delete |

### 3. 权限检查层级

权限检查在多个层面进行，形成纵深防御：

#### 3.1 路由中间件层

在路由组级别进行粗粒度权限检查：

**定义位置**：[Kernel.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Http/Kernel.php#L190-L192)

```php
'permission' => \Laratrust\Middleware\LaratrustPermission::class,
```

**使用示例**：
- `admin` 路由组需要 `permission:read-admin-panel`
- `portal` 路由组需要 `permission:read-client-portal`
- `api` 路由组需要 `permission:read-api`

#### 3.2 控制器方法层

控制器通过 `Permissions` trait 的 `assignPermissionsToController()` 方法自动分配 CRUD 权限：

**调用位置**：[Controller.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Abstracts/Http/Controller.php#L29-L32)

```php
public function __construct()
{
    $this->assignPermissionsToController();
}
```

**自动权限映射**（[Permissions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Traits/Permissions.php#L495-L500)）：

| 权限 | 控制器方法 |
|------|-----------|
| `create-{controller}` | create, store, duplicate, import |
| `read-{controller}` | index, show, edit, export |
| `update-{controller}` | update, enable, disable |
| `delete-{controller}` | destroy |

#### 3.3 菜单项层

菜单项在显示前会检查用户权限：

**检查方法**：[canAccessMenuItem()](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Traits/Permissions.php#L502-L513)

```php
public function canAccessMenuItem($title, $permissions)
{
    $permissions = Arr::wrap($permissions);

    $item = new \stdClass();
    $item->title = $title;
    $item->permissions = $permissions;

    event(new \App\Events\Menu\ItemAuthorizing($item));

    return user()->canAny($item->permissions);
}
```

**授权事件**：`ItemAuthorizing` 事件允许其他模块修改菜单项的权限要求。

### 4. 内置角色与权限

**初始权限种子**：[Permissions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/database/seeds/Permissions.php)

#### 4.1 管理员 (admin)
拥有所有权限，包括：
- 用户管理、公司管理
- 所有业务功能 (销售、采购、银行等)
- 系统设置、模块管理
- 报表查看

#### 4.2 经理 (manager)
拥有大部分业务权限，但不包括：
- 用户管理
- API 访问
- 模块管理

#### 4.3 会计 (accountant)
只读权限为主，包括：
- 查看仪表盘、报表
- 查看销售、采购、银行数据
- 无创建、更新、删除权限

#### 4.4 客户 (customer)
门户访问权限：
- `read-client-portal`
- 查看发票、付款
- 管理个人资料

### 5. 模块权限管理

#### 5.1 模块权限创建

模块安装时会自动创建权限并分配给角色：

**安装监听器模板**：[install.stub](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Console/Stubs/Modules/listeners/install.stub)

```php
class FinishInstallation
{
    use Permissions;

    public $alias = 'blog';

    public function handle(Event $event)
    {
        if ($event->alias != $this->alias) {
            return;
        }

        $this->updatePermissions();
    }

    protected function updatePermissions()
    {
        // c=create, r=read, u=update, d=delete
        $this->attachPermissionsToAdminRoles([
            $this->alias . '-main' => 'c,r,u,d',
        ]);
    }
}
```

#### 5.2 权限辅助方法

`Permissions` trait 提供了丰富的权限操作方法：

| 方法 | 说明 |
|------|------|
| `attachPermissionsToAdminRoles()` | 向管理员角色附加权限 |
| `attachPermissionsToPortalRoles()` | 向门户角色附加权限 |
| `attachPermissionsToAllRoles()` | 向所有角色附加权限 |
| `detachPermissionsFromAdminRoles()` | 从管理员角色移除权限 |
| `createPermission()` | 创建权限 |
| `createRole()` | 创建角色 |
| `attachDefaultModulePermissions()` | 附加模块默认权限 (报表、小部件、设置) |

**Permissions Trait**：[Permissions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Traits/Permissions.php)

#### 5.3 特殊模块权限

系统为模块的特殊功能提供了自动权限创建：

- **报表权限** (`attachModuleReportPermissions`)
- **小部件权限** (`attachModuleWidgetPermissions`)
- **设置权限** (`attachModuleSettingPermissions`)

---

## 模块启用流程

### 1. 模块状态管理

模块启用状态由 `ModuleActivator` 管理，基于数据库存储每个公司的模块状态：

**激活器类**：[ModuleActivator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Utilities/ModuleActivator.php)

**数据库模型**：`Module` 模型存储每个公司的模块启用状态

```
modules 表结构：
- id
- company_id     // 支持多租户，每个公司独立管理模块
- alias          // 模块别名
- enabled        // 是否启用
- created_from   // 创建来源
- created_at
- updated_at
```

### 2. 模块启用流程

```
用户点击启用
    ↓
触发 EnableModule Job
    ↓
执行 module:enable 命令
    ↓
ModuleActivator::enable()
    ↓
更新数据库 modules 表
    ↓
触发 Module\Enabled 事件
    ↓
清除模块缓存
    ↓
菜单中间件重新构建菜单 (下次请求)
```

**Enable Job**：[EnableModule.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Jobs/Install/EnableModule.php)

**Enabled 事件**：[Enabled.php](file:///d:/fz/0601-1\solo-dogfeeding\code\26-akaunting/app/Events/Module/Enabled.php)

### 3. 缓存机制

模块状态和菜单使用缓存提高性能：

**缓存清除监听器**：[ClearCache.php](file:///d:/fz/0601-1/solo-dogfeeding/code/26-akaunting/app/Listeners/Module/ClearCache.php)

监听以下事件清除缓存：
- `Module\Copied` - 模块复制后
- `Module\Enabled` - 模块启用后
- `Module\Disabled` - 模块禁用后
- `Module\Uninstalled` - 模块卸载后

---

## 模块开发规范

### 1. 目录结构

```
modules/
└── Blog/                          # 模块目录 (StudlyCase)
    ├── Providers/
    │   ├── Event.php              # 事件服务提供者 (启用自动发现)
    │   └── Route.php              # 路由服务提供者
    ├── Listeners/
    │   ├── Menu/
    │   │   └── ShowInAdmin.php    # 菜单注入监听器
    │   └── FinishInstallation.php # 安装完成监听器 (创建权限)
    ├── Routes/
    │   ├── admin.php              # 后台路由
    │   └── portal.php             # 门户路由
    ├── Http/
    │   └── Controllers/
    │       └── Posts.php          # 控制器 (自动权限检查)
    ├── Database/
    │   ├── Migrations/
    │   └── Seeds/
    ├── Resources/
    │   ├── views/
    │   └── lang/
    └── module.json                # 模块配置
```

### 2. 菜单注入最佳实践

1. **使用权限检查**：菜单项显示前必须检查权限
2. **设置排序值**：使用 order 参数控制菜单位置
3. **使用图标**：提供 Material Icons 名称或自定义图标
4. **遵循命名约定**：监听器命名为 `ShowIn{MenuType}`

### 3. 权限定义最佳实践

1. **模块前缀**：权限名称以模块别名开头 (如 `blog-posts`)
2. **CRUD 完整**：为每个资源提供完整的 CRUD 权限
3. **角色分配**：明确哪些角色拥有哪些权限
4. **安装时创建**：在 `FinishInstallation` 监听器中创建权限
5. **卸载时清理**：在卸载时移除相关权限

---

## 总结

### 菜单注入机制核心要点

1. **事件驱动**：通过 `AdminCreated` / `AdminCreating` 等事件实现松耦合的菜单扩展
2. **中间件触发**：菜单在 `AdminMenu` 中间件中构建，每次请求都会重建
3. **权限感知**：每个菜单项都关联权限，无权限的用户看不到对应菜单
4. **多种菜单**：支持后台菜单、门户菜单、设置菜单、个人资料菜单等多种类型

### 权限边界核心要点

1. **多层防护**：路由中间件 → 控制器方法 → 菜单项，形成多层权限检查
2. **命名约定**：`{action}-{module}-{resource}` 格式的权限命名
3. **自动映射**：控制器自动根据方法名映射到对应权限
4. **角色管理**：基于角色的权限分配，支持多角色
5. **多租户**：每个公司独立的模块启用状态和权限配置

### 模块与系统的集成点

| 集成点 | 方式 | 关键文件/类 |
|-------|------|------------|
| 菜单注入 | 监听菜单事件 | `App\Events\Menu\AdminCreated` |
| 权限定义 | 安装时创建权限 | `App\Traits\Permissions` |
| 路由注册 | Route facade macro | `App\Providers\Route` |
| 状态管理 | ModuleActivator | `App\Utilities\ModuleActivator` |
| 事件系统 | 自动事件发现 | `shouldDiscoverEvents()` |

这种架构设计使得模块可以独立开发和部署，同时能够无缝地集成到主系统的菜单、权限和路由体系中。
