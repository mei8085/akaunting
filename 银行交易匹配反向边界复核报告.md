# Akaunting 银行交易匹配反向边界复核报告

> **反证式复核**：逐条核验前序结论，揭示隐藏的代码路径和逻辑偏差

---

## 1. 核心结论核验

### 1.1 结论1核验：删除已匹配交易后状态不会回滚

**前序结论**：删除已匹配交易后，账单的 `status` 字段不会自动更新，存在数据不一致。

**实际代码行为**：

**发现隐藏的 Observer** (`app/Observers/Transaction.php:23-59`):

```php
public function deleted(Model $transaction)
{
    if (! empty($transaction->document_id)) {
        $type = ($transaction->type == 'income') ? Document::INVOICE_TYPE : Document::BILL_TYPE;
        $this->updateDocument($transaction, $type);
    }
}

protected function updateDocument($transaction, $type)
{
    $document = $transaction->{$type};
    
    if (empty($document)) {
        return;
    }
    
    $document->transactions_count = $document->transactions->count();
    
    event(new TransactionsCounted($document));
    
    $document->status = ($type == Document::INVOICE_TYPE) ? 'sent' : 'received';
    
    if ($document->transactions_count > 0) {
        $document->status = 'partial';
    }
    
    unset($document->transactions_count);
    
    $document->save();
}
```

**Observer 注册** (`app/Providers/Observer.php:27`):
```php
Transaction::observe('App\Observers\Transaction');
```

**核验结果**：❌ **前序结论错误**

| 路径 | 说明 |
|------|------|
| `Transaction::deleted` | Eloquent 模型事件触发 |
| `Observer::deleted()` | 监听删除事件，检查 `document_id` |
| `updateDocument()` | 重新计算交易条数，更新 `status` |
| `$document->save()` | 持久化状态 |

> **修正结论**：删除已匹配交易后，状态**会**通过 Observer 自动回滚，但回滚策略基于**交易条数**而非金额。

---

### 1.2 结论2核验：paid 始终动态聚合

**前序结论**：`$bill->paid` 是动态计算的，每次访问都会累加关联交易的金额。

**实际代码行为** (`app/Models/Document/Document.php:373-407`):

```php
public function getPaidAttribute()
{
    if (empty($this->amount)) {
        return false;
    }

    // 【关键】当 status == 'paid' 时，直接返回账单金额，不动态计算！
    if ($this->status == 'paid' ) {
        return $this->amount;
    }

    $paid = 0;
    
    $code = $this->currency_code;
    $rate = $this->currency_rate;
    $precision = currency($code)->getPrecision();

    // Lazy eager load transactions if not already loaded
    if (!$this->relationLoaded('transactions')) {
        $this->load('transactions');
    }

    if ($this->transactions->count()) {
        foreach ($this->transactions as $transaction) {
            $amount = $transaction->amount;
            
            if ($code != $transaction->currency_code) {
                $amount = $this->convertBetween($amount, $transaction->currency_code, $transaction->currency_rate, $code, $rate);
            }
            
            $paid += $amount;
        }
    }

    return round($paid, $precision);
}
```

**核验结果**：⚠️ **前序结论部分错误**

| 场景 | paid 属性行为 |
|------|--------------|
| `status != 'paid'` | ✅ 动态累加所有关联交易金额 |
| `status == 'paid'` | ❌ 直接返回 `$this->amount`，不做任何计算 |

**设计意图推测**：性能优化。一旦账单标记为 `paid`，就认为已全额支付，无需每次遍历交易。

**风险**：如果 `status` 与实际交易金额不一致（如数据被篡改），`paid` 属性会返回错误值。

---

## 2. 三层口径差异矩阵：document_id 限制

### 2.1 编辑操作 (Update)

| 层级 | 代码位置 | document_id 检查 | 其他检查 |
|------|----------|------------------|----------|
| Web 控制器 | `app/Http/Controllers/Banking/Transactions.php:250` | `if ($transaction->document_id)` → 拒绝 | - |
| API 控制器 (通用) | `app/Http/Controllers/Api/Banking/Transactions.php:64` | `if ($request->has('document_id'))` → 拒绝 | - |
| API 控制器 (文档关联) | `app/Http/Controllers/Api/Document/DocumentTransactions.php:83-92` | 无检查（专用API，通过 `UpdateBankingDocumentTransaction` Job） | - |
| Job (通用) | `app/Jobs/Banking/UpdateTransaction.php:65-76` | ❌ 无检查 | `reconciled`、`isTransferTransaction()` |
| Job (文档关联) | `app/Jobs/Banking/UpdateBankingDocumentTransaction.php` | 无检查（专用Job，允许修改） | - |

### 2.2 删除操作 (Delete)

| 层级 | 代码位置 | document_id 检查 | 其他检查 |
|------|----------|------------------|----------|
| Web 控制器 | `app/Http/Controllers/Banking/Transactions.php:289-306` | ❌ 无检查 | - |
| API 控制器 (通用) | `app/Http/Controllers/Api/Banking/Transactions.php:81-83` | `if ($transaction->document_id)` → 拒绝 | - |
| API 控制器 (文档关联) | `app/Http/Controllers/Api/Document/DocumentTransactions.php:101-108` | 无检查（专用API，允许删除） | - |
| Job | `app/Jobs/Banking/DeleteTransaction.php:32-43` | ❌ 无检查 | `reconciled`、`isTransferTransaction()` |

### 2.3 差异总结表

| 操作 | 调用路径 | document_id 限制 | 可删除已关联交易？ |
|------|----------|------------------|-------------------|
| 编辑 | Web → Controller → UpdateTransaction Job | ✅ 控制器拦截 | - |
| 编辑 | API(通用) → UpdateTransaction Job | ✅ 请求参数拦截 | - |
| 编辑 | API(文档) → UpdateBankingDocumentTransaction Job | ❌ 无限制 | - |
| 删除 | Web → Controller → DeleteTransaction Job | ❌ 无限制 | ✅ 可以 |
| 删除 | API(通用) → DeleteTransaction Job | ✅ 控制器拦截 | ❌ 不可以 |
| 删除 | API(文档) → DeleteTransaction Job | ❌ 无限制 | ✅ 可以 |

> **关键发现**：
> 1. **Web 端删除**没有 `document_id` 检查，已关联账单的未对账交易可以通过 Web 界面删除
> 2. **API 通用删除**有 `document_id` 检查，拦截已关联交易的删除
> 3. **API 文档专用删除**没有检查，允许删除
> 4. **Job 层**始终没有 `document_id` 检查，完全依赖上层调用者

---

## 3. 删除后状态回写策略分析

### 3.1 回写逻辑详解

**核心代码** (`app/Observers/Transaction.php:36-59`):

```php
protected function updateDocument($transaction, $type)
{
    $document = $transaction->{$type};
    
    if (empty($document)) {
        return;
    }
    
    // 步骤1：统计剩余关联交易的条数
    $document->transactions_count = $document->transactions->count();
    
    event(new TransactionsCounted($document));
    
    // 步骤2：设置默认状态（无交易时）
    $document->status = ($type == Document::INVOICE_TYPE) ? 'sent' : 'received';
    
    // 步骤3：如果还有交易，覆盖为 partial
    if ($document->transactions_count > 0) {
        $document->status = 'partial';
    }
    
    unset($document->transactions_count);
    
    $document->save();
}
```

### 3.2 状态转移矩阵

| 删除前交易数 | 删除后交易数 | 原状态 | 新状态 |
|-------------|-------------|--------|--------|
| 3 | 2 | `paid`/`partial` | `partial` |
| 2 | 1 | `paid`/`partial` | `partial` |
| 1 | 0 | `paid`/`partial` | `sent`/`received` |

### 3.3 按条数划分的语义偏差

**策略**：仅根据 `transactions_count > 0` 判断，不考虑金额。

**问题场景1：金额恰好付清但状态错误标记为 partial**

| 项目 | 值 |
|------|----|
| 账单金额 | $1000 |
| 交易1（删除前） | $700 |
| 交易2（保留） | $300 |
| 删除前状态 | `paid` |
| 删除后交易数 | 1 > 0 |
| 删除后状态 | ❌ `partial` |
| 实际已付金额 | $300（≠ $1000） |

**问题场景2：多条交易金额总和恰好付清**

| 项目 | 值 |
|------|----|
| 账单金额 | $1000 |
| 交易1（保留） | $600 |
| 交易2（保留） | $400 |
| 交易3（删除） | $200 |
| 删除前状态 | `paid` |
| 删除后交易数 | 2 > 0 |
| 删除后状态 | ❌ `partial` |
| 实际已付金额 | $1000（== $1000，应该为 paid） |

**问题场景3：零金额交易残留**

| 项目 | 值 |
|------|----|
| 账单金额 | $1000 |
| 交易1（删除） | $1000 |
| 交易2（保留，金额为0） | $0 |
| 删除前状态 | `paid` |
| 删除后交易数 | 1 > 0 |
| 删除后状态 | ❌ `partial` |
| 实际已付金额 | $0（应该为 sent/received） |

**问题场景4：正负金额抵消**

| 项目 | 值 |
|------|----|
| 账单金额 | $1000 |
| 交易1（删除） | $1000 |
| 交易2（保留） | +$500 |
| 交易3（保留） | -$500 |
| 删除前状态 | `paid` |
| 删除后交易数 | 2 > 0 |
| 删除后状态 | ❌ `partial` |
| 实际已付金额 | $0（应该为 sent/received） |

### 3.4 语义偏差总结

| 判定维度 | 基于条数策略 | 基于金额策略（预期） |
|---------|-------------|-------------------|
| `count == 0` | `sent`/`received` | `sent`/`received` |
| `count > 0 && paid < amount` | ❌ `partial` | `partial` |
| `count > 0 && paid == amount` | ❌ `partial` | `paid` |
| `count > 0 && paid == 0` | ❌ `partial` | `sent`/`received` |

> **根本问题**：状态是业务语义（是否已支付），但回写策略仅判断数据存在性（是否有交易记录），两者语义不对齐。

---

## 4. 可验证的边界案例

### 边界案例1：Web 端删除已关联交易（验证权限差异）

**操作步骤**：
1. 创建账单 $1000，状态 `sent`
2. 创建银行交易 $300，匹配到该账单 → 账单状态 `partial`，交易 `document_id` 非空
3. 在 Web 界面交易列表中找到该交易，点击删除
4. 观察账单状态变化

**预期结果**：
- ✅ 交易删除成功（因为 Web 控制器没有 `document_id` 检查）
- ✅ 账单状态变为 `sent`（Observer 回滚，交易数为0）

**实际验证点**：
- 检查 `Api\Banking\Transactions::destroy()` 有 `document_id` 检查，而 `Banking\Transactions::destroy()` 没有

---

### 边界案例2：API 通用删除被拦截（验证权限差异）

**操作步骤**：
1. 同案例1，创建已关联交易
2. 调用 API `DELETE /api/banking/transactions/{id}`
3. 观察返回结果

**预期结果**：
- ❌ 删除被拒绝，返回错误：`transactions.messages.delete_document_transaction_error`
- 交易仍存在，账单状态仍为 `partial`

**实际验证点**：
- 检查 `app/Http/Controllers/Api/Banking/Transactions.php:81-83` 的拦截逻辑

---

### 边界案例3：删除后状态按条数而非金额（验证语义偏差）

**操作步骤**：
1. 创建账单 $1000，状态 `sent`
2. 创建交易 T1: $600，匹配 → 账单 `partial`
3. 创建交易 T2: $400，匹配 → 账单 `paid`（总金额 $1000）
4. 删除交易 T1

**观察结果**：
- 删除后交易数 = 1 > 0
- ❌ 账单状态变为 `partial`（按条数策略）
- 但实际已付金额 = $400 < $1000（正确，应为 `partial`）

**调整步骤**：
1. 创建账单 $1000
2. 交易 T1: $1000，匹配 → 账单 `paid`
3. 交易 T2: $0，匹配 → 账单仍 `paid`（金额没变）
4. 删除交易 T1

**观察结果**：
- 删除后交易数 = 1 > 0
- ❌ 账单状态变为 `partial`（按条数策略）
- 但实际已付金额 = $0（应该为 `sent`）

---

### 边界案例4：status=paid 时 paid 属性不动态计算

**操作步骤**：
1. 创建账单 $1000，状态 `sent`
2. 创建交易 $1000，匹配 → 账单 `paid`
3. 直接修改数据库，将交易 `document_id` 设为 null（绕过 Eloquent 事件）
4. 访问 `$bill->paid`

**预期结果**：
- ❌ `$bill->paid` 返回 $1000（因为 `status == 'paid'`，直接返回 `amount`）
- ✅ 但实际已无关联交易，动态计算应该返回 $0

**验证代码**：
```php
$bill = Document::find($billId);
echo $bill->status;  // 'paid'
echo $bill->paid;    // 1000（错误！）
echo $bill->transactions->count();  // 0

// 强制重新计算
$bill->status = 'partial';
echo $bill->paid;    // 0（正确）
```

---

### 边界案例5：Observer 仅触发于模型删除，不触发于批量删除

**操作步骤**：
1. 创建账单 $1000，匹配交易 $500 → 账单 `partial`
2. 使用 Query Builder 批量删除（不触发模型事件）：
   ```php
   Transaction::where('document_id', $billId)->delete();
   ```

**预期结果**：
- ❌ 交易已删除，但 Observer 未触发
- ❌ 账单状态仍为 `partial`
- `$bill->paid` 返回 $0（动态计算正确），但 `$bill->status` 为 `partial`（错误）

---

## 5. 复核总结

### 5.1 前序结论修正表

| 前序结论 | 复核结果 | 修正后结论 |
|---------|---------|-----------|
| 删除后状态不会回滚 | ❌ 错误 | 通过 Transaction Observer 会回滚，但基于条数策略 |
| paid 始终动态聚合 | ⚠️ 部分错误 | status != 'paid' 时动态聚合，status == 'paid' 时直接返回 amount |
| 已关联账单交易不可删除 | ⚠️ 部分错误 | API 通用删除不可删除，但 Web 删除和 API 文档专用删除可以删除 |

### 5.2 关键发现清单

1. **隐藏的 Observer**：`app/Observers/Transaction.php` 是状态回滚的核心，极易被遗漏
2. **三层权限不一致**：Web/API/Job 对 `document_id` 的检查不统一，造成行为差异
3. **条数策略偏差**：状态回滚仅看交易条数，不看金额，语义不一致
4. **paid 属性捷径**：`status == 'paid'` 时直接返回金额，可能与实际交易不一致
5. **批量删除风险**：Query Builder 批量删除不触发模型事件，Observer 失效

### 5.3 代码缺陷优先级

| 缺陷 | 严重程度 | 影响 |
|------|---------|------|
| 状态回滚基于条数而非金额 | 高 | 状态语义不准确，误导用户 |
| 三层权限检查不一致 | 中 | 行为可预测性差 |
| paid 属性捷径风险 | 中 | 极端场景下数据不一致 |
| 批量删除不触发 Observer | 中 | 数据一致性依赖调用方式 |
