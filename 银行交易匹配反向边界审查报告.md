# Akaunting 银行交易匹配反向边界审查报告

## 1. 核心误解澄清：前后端权限边界不一致

### 1.1 之前的错误理解

> **错误观点**：已关联账单 (`document_id != null`) 的交易不可删除，这是硬性限制。

### 1.2 实际代码行为

让我们对比前端UI限制和后端实际权限校验：

#### 编辑操作的三层校验

| 层级 | 代码位置 | 校验条件 |
|------|----------|----------|
| 前端按钮显示 | `app/Models/Banking/Transaction.php:584` | `!$this->reconciled && empty($this->document_id) && $this->isNotTransferTransaction()` |
| 控制器层 | `app/Http/Controllers/Banking/Transactions.php:250` | `if ($transaction->document_id)` → 拒绝 |
| 后端Job授权 | `app/Jobs/Banking/UpdateTransaction.php:65-76` | `$this->model->reconciled` 或 `$this->model->isTransferTransaction()` → 拒绝 |

**关键发现**：
- 前端按钮有3个限制条件（已对账、已关联账单、转账）
- 后端Job只检查2个条件（已对账、转账），**没有检查 `document_id`**
- 但控制器层补上了 `document_id` 检查，所以编辑操作实际上是3个限制

#### 删除操作的两层校验

| 层级 | 代码位置 | 校验条件 |
|------|----------|----------|
| 前端按钮显示 | `app/Models/Banking/Transaction.php:707` | `if (! $this->reconciled)` |
| 后端Job授权 | `app/Jobs/Banking/DeleteTransaction.php:32-43` | `$this->model->reconciled` 或 `$this->model->isTransferTransaction()` → 拒绝 |

**关键发现**：
- 前端按钮只检查1个条件（已对账）
- 后端Job检查2个条件（已对账、转账），比前端更严格
- **两者都没有检查 `document_id`**！

### 1.3 结论：已关联账单的交易实际上是可以删除的

```php
// DeleteTransaction.php:32-43
public function authorize(): void
{
    // 只检查这两个条件
    if ($this->model->reconciled) {
        throw new \Exception(trans('messages.warning.reconciled_tran'));
    }

    if ($this->model->isTransferTransaction()) {
        throw new \Exception(trans('messages.error.transfer_transaction'));
    }
    
    // 没有检查 document_id！
}
```

**行为总结**：
| 场景 | 可编辑 | 可删除 |
|------|--------|--------|
| 普通未对账交易 | ✅ | ✅ |
| 已关联账单未对账 | ❌ (控制器拦截) | ✅ (无任何拦截!) |
| 已对账交易 | ❌ | ❌ |
| 转账交易 | ❌ | ❌ |

> **重要**：已关联账单的未对账交易可以被删除！这是之前分析的重大遗漏。

---

## 2. 未处理的边界：已匹配交易删除后的状态不一致

### 2.1 问题描述

当一笔已关联账单的交易被删除时，系统存在严重的数据不一致问题。

### 2.2 账单状态机制分析

**动态计算的 `paid` 属性** (`app/Models/Document/Document.php:373-407`):
```php
public function getPaidAttribute()
{
    // ...
    // 动态累加所有关联交易的金额
    if ($this->transactions->count()) {
        foreach ($this->transactions as $transaction) {
            $amount = $transaction->amount;
            // 币种转换...
            $paid += $amount;
        }
    }
    return round($paid, $precision);
}
```

**静态存储的 `status` 字段**：
- 账单的 `status` 字段存储在数据库中
- 可能值：`draft`, `sent`, `partial`, `paid`, `cancelled`

### 2.3 问题场景复现

1. **初始状态**：
   - 账单金额：$1000，`status = 'partial'`，`paid = 300`
   - 关联交易T1：$300，`document_id = 账单ID`

2. **删除交易**：
   - 调用 `DeleteTransaction` 删除T1
   - 交易记录被删除，账单的 `transactions` 关系自动解除

3. **删除后的状态**：
   | 属性 | 值 | 说明 |
   |------|----|------|
   | `$bill->paid` | $0 | 动态计算，正确 |
   | `$bill->status` | `'partial'` | 静态存储，错误！ |

### 2.4 根本原因

**缺失的监听器**：
- 系统触发了 `TransactionDeleted` 事件 (`app/Jobs/Banking/DeleteTransaction.php:24`)
- 但没有任何监听器监听此事件来重新计算账单状态

```php
// DeleteTransaction.php:24
event(new TransactionDeleted($this->model));

// 但是：没有对应的 Listener 处理这个事件！
```

**对比正常匹配流程** (`app/Jobs/Banking/MatchBankingDocumentTransaction.php`):
```php
protected function checkAmount(): bool
{
    // ...
    // 明确设置状态
    $this->model->status = ($compare === 0) ? 'paid' : 'partial';
    // ...
}
```

匹配时会设置 `status`，但删除时不会回滚。

### 2.5 影响范围

1. **账单列表显示错误**：已完全未付的账单仍显示为 `partial` 或 `paid`
2. **查询统计错误**：`scopePaid()` 和 `scopeNotPaid()` 基于 `status` 字段，统计结果失真
3. **后续匹配异常**：用户尝试重新匹配时，可能因为状态判断错误导致逻辑混乱

---

## 3. 匹配逻辑中的遗漏分支

### 3.1 部分匹配后的继续匹配边界

**场景**：
- 账单金额 $1000，`status = 'partial'`，已付 $300
- 新到账交易 $700，尝试匹配同一账单

**现有逻辑** (`MatchBankingDocumentTransaction.php:59-62`):
```php
$this->model->paid_amount = $this->model->paid;
event(new PaidAmountCalculated($this->model));

$total_amount = round($this->model->amount - $this->model->paid_amount, $document_precision);
```

这里 `$this->model->paid` 是动态计算的，理论上是正确的。

**但存在隐患**：
- 如果之前的 `status` 不正确（如被删除交易但未更新状态），`PaidAmountCalculated` 事件可能依赖错误的上下文
- 没有任何地方确保在匹配前重新验证账单状态的正确性

### 3.2 拆分交易匹配后的状态聚合

**场景**：
- 账单A $500，账单B $300
- 银行交易 $800，拆分为 $500 和 $300 分别匹配

**现有逻辑** (`SplitTransaction.php:26-36`):
```php
foreach ($this->request->items as $item) {
    $transaction = $this->model->duplicate();
    $transaction->split_id = $this->model->id;
    $transaction->amount = $item['amount'];
    $transaction->save();
    
    $document = Document::find($item['document_id']);
    $this->dispatch(new MatchBankingDocumentTransaction($document, $transaction));
}
```

每个子交易独立匹配并设置各自账单的状态，这部分是正确的。

**但遗漏了**：
- 原交易类型被改为 `*-split`，但没有检查原交易是否已关联账单
- 如果原交易在拆分前已关联账单，拆分后原交易的 `document_id` 仍然存在，造成数据不一致

### 3.3 交易金额修改后的状态重新计算

**现有机制** (`UpdateBankingDocumentTransaction.php:92-93`):
```php
// if you edit transaction before remove transaction amount
$this->model->paid_amount = ($this->model->paid - $this->transaction->amount_for_document);
event(new PaidAmountCalculated($this->model));
```

这个Job专门用于更新已关联账单的交易，会正确地：
1. 先减去当前交易金额
2. 重新计算未付金额
3. 重新设置账单状态

**但问题是**：
- 这个Job的调用路径不明确，普通的交易更新走的是 `UpdateTransaction`，不会触发状态重算
- 只有通过特定UI路径（账单页面的支付记录编辑）才会调用此Job

---

## 4. 排查匹配问题时的关键遗漏分支

### 4.1 遗漏分支1：交易删除导致的状态残留

**排查时容易忽略**：
- 看到账单 `status = 'paid'`，但 `$bill->paid = 0`
- 不会想到是因为关联交易被删除了

**排查路径应该是**：
```
账单状态异常 → 检查关联交易数量 → 发现交易数为0 → 检查是否有交易被删除 → 验证status与paid是否一致
```

### 4.2 遗漏分支2：控制器与Job权限不一致

**排查时容易忽略**：
- 以为已关联账单的交易不能删除，但实际上可以
- 删除后导致的连锁问题难以追溯

**排查路径应该是**：
```
账单状态异常 → 检查最近删除的交易 → 查看是否有关联此账单的交易被删除
```

### 4.3 遗漏分支3：动态属性与静态字段的不同步

**排查时容易忽略**：
- `$bill->paid` 是动态计算的，永远正确
- `$bill->status` 是静态存储的，可能过时
- 两者可能不一致

**验证方法**：
```php
$bill = Document::find($id);
$calculated_paid = $bill->paid;  // 动态计算
$stored_status = $bill->status;  // 静态存储

// 检查一致性
$expected_status = ($calculated_paid == 0) ? 'sent' : 
                   ($calculated_paid == $bill->amount) ? 'paid' : 'partial';

if ($stored_status != $expected_status) {
    // 发现不一致！
}
```

---

## 5. 代码缺陷总结

### 5.1 缺陷1：缺少交易删除时的账单状态回滚

**位置**：`app/Jobs/Banking/DeleteTransaction.php`

**问题**：删除已关联账单的交易后，账单的 `status` 字段未更新

**建议修复**：
```php
public function handle(): bool
{
    $this->authorize();
    
    // 记录关联的账单ID（删除前获取）
    $document_id = $this->model->document_id;
    
    event(new TransactionDeleting($this->model));
    
    \DB::transaction(function () use ($document_id) {
        $this->deleteRelationships($this->model, ['recurring', 'taxes']);
        $this->model->delete();
        
        // 新增：更新关联账单的状态
        if ($document_id) {
            $document = Document::find($document_id);
            if ($document) {
                $document->paid_amount = $document->paid;
                event(new PaidAmountCalculated($document));
                
                // 重新计算状态
                if ($document->paid_amount == 0) {
                    $document->status = 'sent'; // 或其他合适的初始状态
                } elseif ($document->paid_amount >= $document->amount) {
                    $document->status = 'paid';
                } else {
                    $document->status = 'partial';
                }
                $document->save();
            }
        }
    });
    
    event(new TransactionDeleted($this->model));
    
    return true;
}
```

### 5.2 缺陷2：删除授权与前端显示不一致

**位置**：`app/Models/Banking/Transaction.php:707` 和 `app/Jobs/Banking/DeleteTransaction.php:32-43`

**问题**：前端只检查 `reconciled`，后端还检查 `isTransferTransaction()`，且都不检查 `document_id`

**建议**：
1. 前端删除按钮也应检查 `isTransferTransaction()`
2. 考虑是否需要增加 `document_id` 检查（根据业务需求）

### 5.3 缺陷3：缺少 `TransactionDeleted` 监听器

**问题**：事件已触发但无人处理

**建议**：创建监听器 `UpdateDocumentStatusOnTransactionDeleted`

---

## 6. 总结与建议

### 6.1 核心发现

1. **权限边界不一致**：前端显示限制与后端实际校验存在差异
2. **删除无状态回滚**：已匹配交易删除后，账单状态不会自动修正
3. **动静数据不同步**：`paid` 动态计算但 `status` 静态存储，删除操作会导致不一致
4. **排查路径缺失**：分析匹配问题时容易忽略交易删除这个关键分支

### 6.2 排查匹配问题的完整检查清单

```
✅ 检查交易是否存在
✅ 检查交易的 document_id 是否正确
✅ 检查账单的 status 字段值
✅ 检查账单的 paid 动态计算值
✅ 验证 status 与 paid 是否一致
✅ 检查最近是否有交易被删除
✅ 检查交易是否为转账类型
✅ 检查交易是否已对账
✅ 检查拆分交易的子交易状态
```

### 6.3 业务影响评估

| 场景 | 概率 | 影响 | 严重程度 |
|------|------|------|----------|
| 已关联账单交易被删除 | 中 | 账单状态残留，统计错误 | 高 |
| 部分匹配后删除部分交易 | 中 | 状态不一致 | 高 |
| 拆分交易后原交易残留关联 | 低 | 数据混乱 | 中 |
| 权限边界混淆导致误操作 | 低 | 意外删除 | 中 |
