# 银行流水与系统内交易的多对多匹配过程分析

## 概述

Akaunting 系统中，银行流水（`transactions` 表记录）与系统内业务单据（`documents` 表，如发票 invoice、账单 bill）之间存在一套完整的匹配机制。支持一对一和一对多匹配，同时提供了对账（Reconciliation）作为未匹配交易的兜底路径。

---

## 一、候选筛选阶段

候选筛选的目标是从大量业务单据中，为给定银行流水找出**可能匹配**的候选文档集合，缩小用户选择范围。

### 1.1 后端候选查询（服务器端初筛）

**入口**：[Transactions::dial()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Http/Controllers/Banking/Transactions.php#L392-L430)

筛选逻辑分为以下几层：

#### 第一层：按交易类型确定文档类型

| 银行流水类型 | 对应业务单据 | 查询构建器 |
|---|---|---|
| 收入（income） | 发票（invoice） | `Document::invoice()` 或联系人的 `invoices()` |
| 支出（expense） | 账单（bill） | `Document::bill()` 或联系人的 `bills()` |

```php
// 代码逻辑（Transactions.php L396-L410）
if ($transaction->isIncome() && $transaction->contact->exists) {
    $builder = $transaction->contact->invoices();
}
if ($transaction->isIncome() && !$transaction->contact->exists) {
    $builder = Document::invoice();
}
if ($transaction->isExpense() && $transaction->contact->exists) {
    $builder = $transaction->contact->bills();
}
if ($transaction->isExpense() && !$transaction->contact->exists) {
    $builder = Document::bill();
}
```

#### 第二层：状态与币种硬过滤

在 [Document::scopeNotPaid()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Models/Document/Document.php#L216-L219) 中定义：

```php
public function scopeNotPaid(Builder $query): Builder
{
    return $query->where($this->qualifyColumn('status'), '<>', 'paid');
}
```

最终查询条件：

```php
$documents = $builder->notPaid()                        // 状态：非已付款
    ->where('currency_code', $transaction->currency_code) // 币种：与交易一致
    ->with(['media', 'totals', 'transactions'])          // 预加载关联
    ->get();
```

### 1.2 前端候选过滤（用户交互层二筛）

**组件**：[AkauntingDocumentButton.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/assets/js/components/AkauntingDocumentButton.vue)

在 [setItemList()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/assets/js/components/AkauntingDocumentButton.vue#L183-L206) 方法中，过滤掉已经选中的文档，避免重复选择：

```javascript
if (selected) {
    return; // 跳过已选中项
}
```

同时计算每个候选文档的关键信息：
- `open = amount - paid`：未结金额（即最大可匹配金额）
- 展示格式：`单据编号 | 联系人 | 备注`，并显示金额/已付/未结三列

候选列表还支持关键字搜索（按单据编号、联系人、备注模糊匹配）。

### 1.3 深度解析：部分匹配后单据为何能继续进入候选池

这是理解"多对多"的关键机制。一个业务单据被部分付款（状态为 `partial`）后，仍然可以被另一笔银行流水继续匹配，直到完全付清。

**根本原因：候选池过滤条件只排除 `status = 'paid'` 的单据。**

回看 [Document::scopeNotPaid()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Models/Document/Document.php#L216-L219)：

```php
public function scopeNotPaid(Builder $query): Builder
{
    // 只排除 status 严格等于 'paid' 的记录
    return $query->where($this->qualifyColumn('status'), '<>', 'paid');
}
```

这意味着以下状态的单据都会继续出现在候选池中：

| 状态 | 是否进入候选池 | 说明 |
|---|---|---|
| `draft`（草稿） | ✅ | 尚未进入付款流程 |
| `sent`（已发送）/ `received`（已收到） | ✅ | 完全未付 |
| `partial`（部分付款） | ✅ | **核心：部分付清，仍有未结金额** |
| `overdue`（逾期） | ✅ | 尚未付清，只是超期了 |
| `unpaid`（未付） | ✅ | 标记为未付 |
| `paid`（已付） | ❌ | 完全付清，不再进入候选池 |
| `cancelled`（已取消） | ❌ | 通过 accrued scope 已排除 |

**状态流转示例**：假设一张金额为 $1000 的账单 Bill#2024：

```
初始状态：status = 'received', paid = 0
    │
    ▼ 匹配 $300 银行流水（MatchBankingDocumentTransaction）
status = 'partial', paid = 300, 未结 = 700
    │
    ▼ 账单继续出现在候选池中（notPaid = true）
    ▼ 另一笔 $400 流水再次匹配这张账单
status = 'partial', paid = 700, 未结 = 300
    │
    ▼ 账单仍然继续出现在候选池中
    ▼ 第三笔 $300 流水恰好付清余额
status = 'paid', paid = 1000, 未结 = 0
    │
    ▼ 从此不再出现在候选池中（notPaid = false）
```

这就是"多对多"的另一层含义：**一张业务单据可以被多笔银行流水先后匹配、累计付款**。系统并不限制一张单据只能由一笔流水匹配。

---

## 二、匹配确认阶段

用户在前端弹窗中选择一个或多个候选文档、调整各文档的匹配金额后，提交确认。系统根据选择的文档数量走不同分支。

### 2.1 分发入口

**控制器**：[Transactions::connect()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Http/Controllers/Banking/Transactions.php#L432-L461)

```php
if ($total_items == 1) {
    // 一对一匹配
    $response = $this->ajaxDispatch(new MatchBankingDocumentTransaction($document, $transaction));
}
if ($total_items > 1) {
    // 一对多匹配（拆分交易）
    $response = $this->ajaxDispatch(new SplitTransaction($transaction, $request->data));
}
```

### 2.2 一对一匹配：MatchBankingDocumentTransaction

**Job**：[MatchBankingDocumentTransaction](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/MatchBankingDocumentTransaction.php)

执行流程：

#### Step 1：金额校验（checkAmount）

[checkAmount()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/MatchBankingDocumentTransaction.php#L43-L86)

1. 精度处理：按币种精度对交易金额四舍五入
2. 币种转换：若交易与文档币种不同，通过 `convertBetween()` 换算为文档币种
3. 计算未结金额：`total_amount = document.amount - document.paid`
4. 比较（使用 `bccomp` 高精度比较）：
   - `bccomp(amount, total_amount) === 1`：匹配金额 > 未结金额 → **抛出异常** `messages.error.over_match`
   - `bccomp === 0`：金额恰好 → 文档状态置为 `'paid'`
   - 否则（部分付款）→ 文档状态置为 `'partial'`

#### Step 2：数据库事务内执行

```php
\DB::transaction(function () {
    // 将交易关联到文档
    $this->transaction = $this->dispatch(new UpdateTransaction($this->transaction, [
        'document_id' => $this->model->id,
        'type'        => $this->transaction->type,
    ]));
    // 保存文档（含状态变更）
    $this->model->save();
    // 记录操作历史
    $this->createHistory();
});
```

### 2.3 一对多匹配：SplitTransaction

**Job**：[SplitTransaction](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/SplitTransaction.php)

当用户选择多个文档时，系统将一笔银行流水**拆分为多笔子交易**，分别与各文档匹配。

#### Step 1：前置校验

| 校验项 | 方法 | 规则 | 失败结果 |
|---|---|---|---|
| 金额守恒 | [checkAmount()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/SplitTransaction.php#L60-L84) | Σ子项金额 === 父交易金额 | 抛出 `messages.error.same_amount` |
| 文档存在性 | [authorize()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/SplitTransaction.php#L47-L58) | 每个 item 的 document_id 非空且记录存在 | 抛出 `messages.error.not_found` |

#### Step 2：拆分与匹配（事务内）

```php
DB::transaction(function () {
    foreach ($this->request->items as $item) {
        // 1. 复制父交易为子交易
        $transaction            = $this->model->duplicate();
        $transaction->split_id  = $this->model->id;  // 指向父交易
        $transaction->amount    = $item['amount'];    // 子项金额
        $transaction->save();

        // 2. 逐个执行一对一匹配
        $document = Document::find($item['document_id']);
        $this->dispatch(new MatchBankingDocumentTransaction($document, $transaction));
    }

    // 3. 父交易类型变更为拆分类型（如 expense → expense-split）
    $this->model->type = config('type.transaction.' . $this->model->type . '.split_type');
    $this->model->save();
});
```

**数据结构示意**：
```
transactions 表：
┌────┬───────────┬────────┬────────────┬───────────┐
│ id │ type      │ amount │ document_id│ split_id  │
├────┼───────────┼────────┼────────────┼───────────┤
│ 1  │ expense-split │ 500  │ NULL       │ NULL      │  ← 父交易
│ 2  │ expense   │ 200    │ 101        │ 1         │  ← 子交易1，匹配账单101
│ 3  │ expense   │ 300    │ 102        │ 1         │  ← 子交易2，匹配账单102
└────┴───────────┴────────┴────────────┴───────────┘
```

父交易通过 `splits()` 关联可访问所有子交易。

### 2.4 前端确认门槛

在 [AkauntingConnectTransactions.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/assets/js/components/AkauntingConnectTransactions.vue) 中，[canConfirmConnect](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/assets/js/components/AkauntingConnectTransactions.vue#L372-L384) 计算属性阻止差异不为 0 时提交：

```javascript
canConfirmConnect: function () {
    if (this.form.loading || !this.form.items.length) return false;
    const diff = this.difference_amount;
    // 差异必须小于该币种最小精度单位（如 USD < 0.01）
    return Math.abs(diff) < Math.pow(10, -(this.currency.precision || 2));
}
```

同时，单项金额若超过文档未结金额，[checkAmount()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/assets/js/components/AkauntingConnectTransactions.vue#L500-L511) 会在 50ms 后自动回退到最大允许值。

### 2.5 深度解析：已付金额怎样累加到下一次匹配

业务单据可以被多笔流水累计付款（如 1.3 节所述），核心在于 `paid` 不是数据库字段，而是**动态计算的访问器（Accessor）**，每次匹配时都会实时汇总所有已关联交易的金额。

#### 2.5.1 paid 访问器的计算逻辑

**定义位置**：[Document::getPaidAttribute()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Models/Document/Document.php#L373-L407)

```php
public function getPaidAttribute()
{
    if (empty($this->amount)) return false;
    if ($this->status == 'paid') return $this->amount;  // 已全额付款直接返回

    $paid = 0;
    $code = $this->currency_code;
    $rate = $this->currency_rate;
    $precision = currency($code)->getPrecision();

    // 懒加载关联交易（防止 N+1）
    if (!$this->relationLoaded('transactions')) {
        $this->load('transactions');
    }

    // 遍历所有已关联的交易记录，累加金额
    if ($this->transactions->count()) {
        foreach ($this->transactions as $transaction) {
            $amount = $transaction->amount;

            // 币种不同则按汇率转换为文档币种
            if ($code != $transaction->currency_code) {
                $amount = $this->convertBetween(
                    $amount,
                    $transaction->currency_code,
                    $transaction->currency_rate,
                    $code, $rate
                );
            }

            $paid += $amount;  // ← 累计累加
        }
    }

    return round($paid, $precision);
}
```

**关键点**：`paid` 是通过 `$document->transactions` 关联关系**实时累加**出来的，只要有新的 transaction 通过 `document_id` 关联到该文档，下次访问 `$document->paid` 时就会自动包含这笔金额。

#### 2.5.2 未结金额的计算链路

未结金额 `amount_due` 也是访问器：[Document::getAmountDueAttribute()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Models/Document/Document.php#L457-L462)

```php
public function getAmountDueAttribute()
{
    $precision = currency($this->currency_code)->getPrecision();
    return round($this->amount - $this->paid, $precision);
}
```

#### 2.5.3 匹配时如何利用已付金额做校验

在每次执行匹配（一对一、一对多中的子匹配）时，`checkAmount()` 都会读取当前已累计的 `paid`，确保本次匹配金额不超过剩余未结金额。

**场景 A：一对一新匹配**（[MatchBankingDocumentTransaction::checkAmount()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/MatchBankingDocumentTransaction.php#L43-L86)）

```php
// 1. 读取所有已关联交易的累计金额（动态计算）
$this->model->paid_amount = $this->model->paid;
event(new PaidAmountCalculated($this->model));

// 2. 计算剩余可付金额
$total_amount = round($this->model->amount - $this->model->paid_amount, $document_precision);

// 3. 比较本次匹配金额是否超额
$compare = bccomp($amount, $total_amount, $document_precision);
// 1 → 超额，抛异常
// 0 → 恰好，status = 'paid'
// -1 → 部分，status = 'partial'
```

**场景 B：修改已存在的匹配金额**（[UpdateBankingDocumentTransaction::checkAmount()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/UpdateBankingDocumentTransaction.php#L75-L119)）

这是更精细的场景：用户要修改一笔已经关联到文档的交易金额。此时必须先把"这笔交易本身的金额"从累计已付中扣除，否则会重复计算。

```php
// ↓ 关键差异：先减去当前交易自身的金额（避免重复统计）
$this->model->paid_amount = ($this->model->paid - $this->transaction->amount_for_document);
event(new PaidAmountCalculated($this->model));

// 再计算剩余可付金额
$total_amount = round($this->model->amount - $this->model->paid_amount, $document_precision);
```

其中 `amount_for_document` 也是访问器，将交易金额按文档币种换算后返回（见 [Transaction::getAmountForDocumentAttribute()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Models/Banking/Transaction.php#L370-L384)）。

#### 2.5.4 前端候选列表中的金额展示

在前端候选文档列表中，[AkauntingDocumentButton.vue::setItemList()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/assets/js/components/AkauntingDocumentButton.vue#L183-L206) 也计算了每行的未结金额：

```javascript
this.item_list.push({
    id: item.id,
    name: item.document_number + ' | ' + item.contact_name + ...,
    amount: item.amount,    // 单据总额
    paid: item.paid,        // 已付（后端序列化时由 getPaidAttribute 计算）
    open: item.amount - item.paid,  // ← 未结金额，前端展示
});
```

用户在弹窗中选中该文档时，默认填入的金额正是这个 `open` 值（见 [AkauntingConnectTransactions.vue::onAddItem()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/assets/js/components/AkauntingConnectTransactions.vue#L407-L419)），即默认以"付清剩余未结金额"为匹配金额。

#### 2.5.5 完整累加流程示例

```
单据 Bill#2024，amount = $1000

第 1 次匹配流水 $300：
  document.paid  = 遍历 transactions → 0（尚无关联）
  total_amount   = 1000 - 0 = 1000
  300 ≤ 1000 → 允许
  transaction 表新增一条：document_id=Bill#2024, amount=300
  document.status = 'partial'

第 2 次匹配流水 $400：
  document.paid  = 遍历 transactions → 300（第 1 笔）
  total_amount   = 1000 - 300 = 700
  400 ≤ 700 → 允许
  transaction 表再新增一条：document_id=Bill#2024, amount=400
  document.status = 'partial'

第 3 次匹配流水 $300：
  document.paid  = 遍历 transactions → 300 + 400 = 700
  total_amount   = 1000 - 700 = 300
  300 == 300 → 恰好
  transaction 表再新增一条：document_id=Bill#2024, amount=300
  document.status = 'paid' → 不再进入候选池
```

---

## 三、匹配撤销与恢复机制

匹配并不是"一次性写入"的终态操作。用户可以删除已匹配的交易记录，系统会自动恢复相关单据和父交易的状态。这个机制由 `Transaction Observer`（观察者）监听删除事件后触发。

**核心观察者**：[app/Observers/Transaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Observers/Transaction.php)

### 3.1 删除触发链路

一笔交易的删除会经过以下事件链：

```
用户点击删除
  │
  ▼ [DeleteTransaction Job]
event(new TransactionDeleting($transaction))  // 删除前
  │
  ▼ 实际删除（DB transaction 内）
  $this->model->delete()
  │
  ▼ [Observer 自动触发 deleted() 方法]
  Transaction Observer::deleted($transaction)
  │
  ├─► 如果交易有关联文档 → updateDocument()：恢复单据状态
  └─► 如果交易有 split_id  → updateTransaction()：恢复父交易类型
  │
  ▼
event(new TransactionDeleted($transaction))   // 删除后
```

### 3.2 深度解析：子交易被删除时父交易如何恢复

这是 SplitTransaction（一对多拆分匹配）的反向恢复逻辑。

**触发条件**：被删除的交易的 `split_id` 不为空（即它是某笔父交易拆分出来的子交易）。

**恢复方法**：[Transaction::updateTransaction()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Observers/Transaction.php#L68-L79)

```php
protected function updateTransaction($transaction)
{
    // 1. 通过 split_id 找到父交易
    $splitted_transaction = Model::find($transaction->split_id);

    // 2. 检查父交易是否还有其他子交易
    if ($splitted_transaction->splits->count() == 0) {
        // 3. 所有子交易都已删除 → 恢复父交易的原始类型
        $values = [
            'type' => $transaction->type,  // 使用刚被删除的子交易的原始类型
        ];
        $this->dispatch(new UpdateTransaction($splitted_transaction, $values));
    }
    // 注意：如果父交易还有其他子交易（splits->count() > 0），
    // 则父交易保持 *-split 类型不变
}
```

**恢复规则**：

| 父交易剩余子交易数量 | 父交易状态是否恢复 | 说明 |
|---|---|---|
| 0（全部删除） | ✅ 恢复为原始类型 | 如 `expense-split` → `expense` |
| ≥ 1（还有剩余） | ❌ 保持 `*-split` 类型 | 拆分关系仍然有效 |

这里利用了被删除子交易的 `type` 字段——子交易在创建时继承了父交易拆分前的原始类型（不带 `-split` 后缀），因此可以用它来还原父交易。

**恢复示例**：

```
初始：父交易 T1，type = 'expense'，amount = $500

拆分匹配：T1 → 子交易 T2（$200，匹配账单 A）
              → 子交易 T3（$300，匹配账单 B）
结果：T1.type = 'expense-split'，T2.split_id = T1，T3.split_id = T1

删除 T2：
  T2 被删除
  Observer::deleted() → 检查 T1->splits->count() = 1（还有 T3）
  → 不恢复，T1 仍为 'expense-split'

删除 T3：
  T3 被删除
  Observer::deleted() → 检查 T1->splits->count() = 0（子交易全部删除）
  → 恢复：T1.type = T3.type = 'expense'
  → T1 重新成为可编辑、可再次 connect 的普通交易
```

### 3.3 关联文档的交易被删除时，单据状态如何恢复

**触发条件**：被删除的交易的 `document_id` 不为空（即它关联了业务单据）。

**恢复方法**：[Transaction::updateDocument()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Observers/Transaction.php#L36-L59)

```php
protected function updateDocument($transaction, $type)
{
    // 1. 找到关联的单据（invoice 或 bill）
    $document = $transaction->{$type};
    if (empty($document)) return;

    // 2. 统计该单据剩余的关联交易数量
    $document->transactions_count = $document->transactions->count();
    event(new TransactionsCounted($document));

    // 3. 根据剩余交易数量恢复状态
    $document->status = ($type == Document::INVOICE_TYPE) ? 'sent' : 'received';
    if ($document->transactions_count > 0) {
        $document->status = 'partial';  // 还有其他付款 → 部分付款
    }
    // 否则 → 'sent'/'received'（相当于完全未付）

    $document->save();

    // 4. 记录操作历史
    $this->dispatch(new CreateDocumentHistory($document, 0, $this->getDescription($transaction)));
}
```

| 单据剩余关联交易数量 | 单据恢复后状态 |
|---|---|
| 0（所有关联交易都被删除） | 发票→`sent`，账单→`received`（回到完全未付） |
| ≥ 1（还有其他关联交易） | `partial`（仍为部分付款） |

**注意**：这里的状态恢复是"保守恢复"——只要还有任何关联交易存在，就标记为 `partial`，而不会去精确计算剩余金额是否恰好全额付清。精确的已付金额仍然由 `getPaidAttribute()` 动态计算。

### 3.4 删除限制（安全网）

并非所有交易都可以随意删除。[DeleteTransaction::authorize()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/DeleteTransaction.php#L32-L43) 设置了两道防线：

```php
public function authorize(): void
{
    // 防线 1：已对账的交易不可删除
    if ($this->model->reconciled) {
        throw new \Exception(trans('messages.warning.reconciled_tran'));
    }
    // 防线 2：转账类交易不可直接删除（需要走专门的删除流程）
    if ($this->model->isTransferTransaction()) {
        throw new \Exception(trans('messages.error.transfer_transaction'));
    }
}
```

---

## 四、未匹配项的兜底路径

### 4.1 未匹配交易的存在形式

当交易的 `document_id` 为 `NULL` 时，即为未匹配交易。它仍然是有效的银行流水记录，可以：
- 在 [交易列表](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Http/Controllers/Banking/Transactions.php#L38-L94) 中查看、编辑、删除
- 手动录入（无对应业务单据的零散收支）
- 通过 Excel/CSV [导入](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Imports/Banking/Transactions.php)

### 4.2 兜底核心：银行对账（Reconciliation）

对账是将银行流水与真实银行对账单进行核对的过程，是未匹配交易的最终兜底闭环。

#### 对账流程

**控制器**：[Reconciliations](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Http/Controllers/Banking/Reconciliations.php)

1. **创建对账**（[create()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Http/Controllers/Banking/Reconciliations.php#L55-L70)）：
   - 选择账户、起止日期
   - 系统加载该时间段内的所有交易（[getTransactions()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Http/Controllers/Banking/Reconciliations.php#L184-L192)）
   - 计算期初余额（[getOpeningBalance()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Http/Controllers/Banking/Reconciliations.php#L202-L220)）

2. **勾选核对交易**：
   - 用户逐笔勾选与银行对账单一致的交易（包含匹配和未匹配的）
   - 前端实时计算：`已清算金额 = 期初余额 + Σ勾选收入 - Σ勾选支出`
   - `差额 = 期末余额（银行提供） - 已清算金额`
   - 仅当差额为 0 时，`reconcile = true`，才可确认

3. **确认对账**（[CreateReconciliation](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/CreateReconciliation.php) / [UpdateReconciliation](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/UpdateReconciliation.php)）：

```php
// CreateReconciliation.php L32-L44
if ($reconcile && $transactions) {
    foreach ($transactions as $key => $value) {
        if (empty($value)) continue;
        $t = explode('_', $key);
        $transaction = Transaction::find($t[1]);
        $transaction->reconciled = 1;  // 标记为已对账
        $transaction->save();
    }
}
```

#### 已对账交易的锁定

一旦交易被标记 `reconciled = 1`，即进入"锁定"状态：
- [DeleteTransaction::authorize()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/DeleteTransaction.php#L32-L43) 阻止删除
- [Transactions::update()](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Http/Controllers/Banking/Transactions.php#L248-L261) 阻止修改关联文档的交易
- 语言包 [messages.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/lang/zh-CN/messages.php#L31) 定义了警告：`reconciled_tran` / `reconciled_doc`

### 4.3 兜底路径图示

```
银行流水（Transaction）
    │
    ├─► 能匹配业务单据？
    │       ├─ 是 ──► 一对一 MatchBankingDocumentTransaction
    │       │          └─► 单据状态 = paid/partial
    │       └─ 否 ──► 一对多 SplitTransaction
    │                  └─► 拆分子交易分别匹配
    │
    └─► 无对应单据 / 用户不匹配
            │
            ├─► 交易保留（document_id = NULL）
            │      可编辑、可删除、可后续再 connect
            │
            └─► 银行对账（Reconciliation）兜底
                   ├─► 用户勾选与银行对账单一致
                   ├─► 差额为 0 时确认对账
                   └─► 交易标记 reconciled = 1（锁定）
```

### 4.4 扩展兜底：Bank Feeds 模块

系统预留了 [BankFeeds 小部件](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Widgets/BankFeeds.php) 和 Bank Feeds 模块接口（见 [import.blade.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/views/banking/transactions/import.blade.php#L17)），支持从银行 API 自动拉取流水，导入后同样走上述匹配或对账流程。

---

## 五、关键数据模型字段

### transactions 表

| 字段 | 作用 |
|---|---|
| `document_id` | 关联的业务单据 ID（NULL 表示未匹配） |
| `split_id` | 指向父交易 ID（子交易才有值） |
| `reconciled` | 是否已对账（0/1），1 表示锁定 |
| `type` | 交易类型：income / expense / income-split / expense-split / income-transfer 等 |

### documents 表

| 字段 | 作用 |
|---|---|
| `status` | 单据状态：draft / sent / received / partial / paid / overdue / cancelled |
| `paid` | 访问器：动态汇总所有关联 transactions 的已付金额 |
| `reconciled` | 访问器：所有关联交易都已对账时为 1 |

### reconciliations 表

| 字段 | 作用 |
|---|---|
| `account_id` | 对账的银行账户 |
| `started_at` / `ended_at` | 对账期间 |
| `closing_balance` | 期末余额（银行对账单值） |
| `transactions` | JSON 数组，记录勾选的交易及金额 |
| `reconciled` | 是否确认对账（0=进行中，1=已完成） |

---

## 六、核心类与文件索引

| 层级 | 文件 | 职责 |
|---|---|---|
| 控制器 | [Transactions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Http/Controllers/Banking/Transactions.php) | 交易 CRUD、候选查询 dial()、匹配分发 connect() |
| 控制器 | [Reconciliations.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Http/Controllers/Banking/Reconciliations.php) | 对账流程控制、差额计算 |
| Job | [MatchBankingDocumentTransaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/MatchBankingDocumentTransaction.php) | 一对一匹配执行、金额校验 |
| Job | [UpdateBankingDocumentTransaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/UpdateBankingDocumentTransaction.php) | 修改已有匹配的交易金额、避免重复统计已付 |
| Job | [SplitTransaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/SplitTransaction.php) | 一对多拆分匹配执行 |
| Job | [CreateBankingDocumentTransaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/CreateBankingDocumentTransaction.php) | 从文档直接创建付款交易 |
| Job | [DeleteTransaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/DeleteTransaction.php) | 删除交易（含已对账和转账校验） |
| Job | [UpdateTransaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/UpdateTransaction.php) | 更新交易（含子交易恢复父类型时的调用） |
| Job | [CreateReconciliation.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/CreateReconciliation.php) | 创建对账并标记交易已对账 |
| Job | [UpdateReconciliation.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Jobs/Banking/UpdateReconciliation.php) | 更新对账 |
| Observer | [Transaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Observers/Transaction.php) | 监听交易 deleted 事件，恢复单据状态和父交易类型 |
| 模型 | [Transaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Models/Banking/Transaction.php) | 交易模型、scopes、accessors（amount_for_document 等） |
| 模型 | [Document.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Models/Document/Document.php) | 单据模型、paid/amount_due/reconciled 访问器、notPaid scope |
| 模型 | [Reconciliation.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/app/Models/Banking/Reconciliation.php) | 对账模型 |
| 前端组件 | [AkauntingConnectTransactions.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/assets/js/components/AkauntingConnectTransactions.vue) | 匹配弹窗、金额校验、提交 |
| 前端组件 | [AkauntingDocumentButton.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/assets/js/components/AkauntingDocumentButton.vue) | 候选文档选择器、未结金额展示 |
| 前端 JS | [reconciliations.js](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/assets/js/views/banking/reconciliations.js) | 对账页差额计算 |
| 前端 JS | [global.js](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/resources/assets/js/mixins/global.js) | onConnectTransactions 弹窗触发 |
| 测试 | [SplitTransactionTest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/27-akaunting/tests/Feature/Banking/SplitTransactionTest.php) | 拆分匹配的回归测试用例 |
