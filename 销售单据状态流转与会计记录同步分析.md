# Akaunting 销售单据状态流转与会计记录同步分析

## 1. 核心架构概述

Akaunting 采用 **事件驱动 + 任务调度** 的架构模式来处理销售单据（Invoice）的状态变化与会计记录同步。整个流程围绕 `Document` 模型展开，通过 Laravel 的 Event/Listener 机制实现状态流转与账务写入的解耦。

### 1.1 核心模型关系

```
Document (销售单据)
    ├── DocumentItem (单据行项)
    ├── DocumentTotal (单据合计)
    ├── DocumentHistory (状态变更历史)
    └── Transaction (银行交易/会计记录)
```

## 2. 销售单据状态定义

### 2.1 状态枚举

根据 `app/Traits/Documents.php:69-107`，销售发票（invoice）的状态包括：

| 状态 | 说明 | CSS类 | 实现状态 |
|------|------|-------|---------|
| `draft` | 草稿 | `status-draft` | ✅ 已实现 |
| `sent` | 已发送 | `status-danger` | ✅ 已实现 |
| `viewed` | 已查看 | `status-sent` | ✅ 已实现 |
| `approved` | 已批准 | `status-draft` (default) | ⚠️ 预留未实现 |
| `partial` | 部分付款 | `status-partial` | ✅ 已实现 |
| `paid` | 已付款 | `status-success` | ✅ 已实现 |
| `cancelled` | 已作废 | `status-canceled` | ✅ 已实现 |
| `overdue` | 逾期 | - | 计算属性 |
| `unpaid` | 未付款 | - | 计算属性 |

> **重要说明**：`approved` 状态虽然在状态列表中定义，但在当前代码中**没有任何实际的状态流转逻辑**。代码注释明确提到：`//This control will be removed when approval status is added to documents.`（`app/Listeners/Document/MarkDocumentSent.php:19`），表明这是一个为未来审批功能预留的状态。

### 2.2 状态流转路径（已实现）

```
draft → sent → viewed → partial → paid
           ↓            ↓
        cancelled    cancelled
```

## 3. 单据创建流程

### 3.1 创建流程时序

```
控制器 (Invoices::store)
    ↓
Job: CreateDocument
    ├─ event: DocumentCreating
    ├─ DB::transaction
    │   ├─ Document::create (初始状态: draft)
    │   ├─ 上传附件
    │   ├─ Job: CreateDocumentItemsAndTotals
    │   ├─ 更新 Document
    │   └─ 创建定期单据 (如果需要)
    └─ event: DocumentCreated
```

### 3.2 创建时的事件监听

根据 `app/Providers/Event.php:53-56`，`DocumentCreated` 事件触发以下监听器：

| 监听器 | 职责 | 代码位置 |
|--------|------|---------|
| `CreateDocumentCreatedHistory` | 记录创建历史 | `app/Listeners/Document/CreateDocumentCreatedHistory.php` |
| `IncreaseNextDocumentNumber` | 递增单据编号 | `app/Listeners/Document/IncreaseNextDocumentNumber.php` |
| `SettingFieldCreated` | 处理自定义字段 | `app/Listeners/Document/SettingFieldCreated.php` |

> **关键点**：单据创建时状态为 `draft`，**不会**立即产生会计记录。

## 4. 状态流转与会计记录写入

### 4.1 标记为已发送 (Mark as Sent)

**触发方式**：
- 手动点击"标记为已发送"按钮 → `Invoices::markSent()`
- 发送邮件 → `DocumentSent` 事件

**流程**：
```
event: DocumentMarkedSent / DocumentSent
    ↓
Listener: MarkDocumentSent
    ├─ 检查当前状态 (非 partial/paid 才更新)
    ├─ 金额为0时自动标记为 paid
    ├─ 更新 document.status = 'sent'
    └─ Job: CreateDocumentHistory
```

**代码位置**：`app/Listeners/Document/MarkDocumentSent.php:14-28`

### 4.2 付款处理 - 两条路径对比

Akaunting 存在**两条独立的付款路径**，虽然最终都会创建会计记录，但触发方式、流程和行为有明显差异。

---

#### 路径 A：手工收款（Manual Payment）

**触发点**：
1. Web 界面模态框：`DocumentTransactions::store()` (`app/Http/Controllers/Modals/DocumentTransactions.php:128-143`)
2. API 接口：`Api\Document\DocumentTransactions::store()` (`app/Http/Controllers/Api/Document/DocumentTransactions.php:66-73`)

**流程**：
```
用户提交付款表单
    ↓
控制器直接 dispatch Job
    ↓
Job: CreateBankingDocumentTransaction
    ├─ event: DocumentTransactionCreating
    ├─ prepareRequest()  // 准备交易数据
    ├─ checkAmount()     // 金额校验与状态计算
    │   ├─ 防止超额支付
    │   └─ 设置状态: paid / partial
    ├─ DB::transaction
    │   ├─ Job: CreateTransaction  // 创建银行交易(会计记录)
    │   ├─ 保存 Document (更新状态)
    │   └─ Job: CreateDocumentHistory
    └─ event: DocumentTransactionCreated
```

**特点**：
- ✅ 支持**部分付款**（金额由用户输入）
- ❌ 不自动发送付款通知
- 交易编号：用户输入或自动生成
- 支付方式：用户选择

---

#### 路径 B：回调收款（Payment Gateway Callback）

**触发点**：支付网关回调 → `PaymentController::dispatchPaidEvent()` (`app/Abstracts/Http/PaymentController.php:170-180`)

**流程**：
```
支付网关回调
    ↓
PaymentController::dispatchPaidEvent()
    ├─ 自动填充: amount = invoice.amount (全额)
    ├─ 自动填充: payment_method = 支付网关别名
    ├─ 自动填充: account_id = 支付网关配置账户
    └─ event: PaymentReceived
            ├─ Listener: CreateDocumentTransaction
            │   └─ Job: CreateBankingDocumentTransaction
            │       ├─ event: DocumentTransactionCreating
            │       ├─ prepareRequest()
            │       ├─ checkAmount()  // 总是全额, 状态设为 paid
            │       ├─ DB::transaction
            │       │   ├─ Job: CreateTransaction
            │       │   ├─ 保存 Document
            │       │   └─ Job: CreateDocumentHistory
            │       └─ event: DocumentTransactionCreated
            │
            └─ Listener: SendDocumentPaymentNotification
                ├─ 发送通知给客户 (type: income)
                └─ 发送通知给公司管理员
```

**代码位置**：
- 事件构造：`app/Events/Document/PaymentReceived.php:21-30`（自动生成交易编号）
- 通知发送：`app/Listeners/Document/SendDocumentPaymentNotification.php:16-42`

**特点**：
- ❌ **总是全额付款**（`$request['amount'] = $invoice->amount`）
- ✅ 自动发送付款通知给客户和管理员
- 交易编号：在 `PaymentReceived` 事件中自动生成
- 支付方式：由支付网关决定

---

### 4.3 两条付款路径对比表

| 对比项 | 手工收款 (路径 A) | 回调收款 (路径 B) |
|--------|-------------------|-------------------|
| 触发方式 | 用户主动提交 | 支付网关回调 |
| 调用层级 | 直接调用 Job | Event → Listener → Job |
| 付款金额 | 用户输入，支持部分付款 | 固定为单据全额 |
| 付款状态 | paid / partial | 总是 paid |
| 通知发送 | 不自动发送 | 自动发送给客户和管理员 |
| 交易编号 | 用户输入或自动生成 | 事件中自动生成 |
| 支付方式 | 用户选择 | 支付网关决定 |
| 适用场景 | 现金、银行转账等线下收款 | 在线支付（PayPal、Stripe 等） |

### 4.4 会计记录 (Transaction) 结构

`Transaction` 模型是 Akaunting 的会计记录载体，关键字段：

| 字段 | 说明 |
|------|------|
| `type` | `income` (收入) 或 `expense` (支出) |
| `account_id` | 银行账户ID |
| `document_id` | 关联的单据ID |
| `amount` | 交易金额 |
| `category_id` | 分类ID (用于财务报表) |
| `payment_method` | 支付方式 |
| `reconciled` | 是否已对账 |

> **设计特点**：Akaunting 采用**单式记账**而非复式记账，通过 `type` 字段区分收支方向，不直接维护借贷方科目。

## 5. 作废场景处理边界

### 5.1 作废流程

```
控制器: Invoices::markCancelled()
    ↓
event: DocumentCancelled
    ↓
Listener: MarkDocumentCancelled
    ├─ Job: CancelDocument
    │   ├─ authorize()  // 安全检查
    │   └─ DB::transaction
    │       ├─ 删除关联 transactions (银行交易)
    │       ├─ 删除 recurring (定期单据)
    │       └─ 更新 status = 'cancelled'
    └─ Job: CreateDocumentHistory
```

### 5.2 作废的边界条件

**UI 显示条件**（`app/Models/Document/Document.php:717`）：
```php
if (! in_array($this->status, ['cancelled', 'draft'])) {
    // 显示作废按钮
}
```
即：`sent`、`viewed`、`partial`、`paid` 状态都会显示作废按钮。

**实际禁止条件**（`app/Jobs/Document/CancelDocument.php:41-49`）：
```php
public function authorize(): void
{
    // 禁止作废已对账的单据
    if ($this->model->transactions()->isReconciled()->count()) {
        throw new \Exception($message);
    }
}
```

**作废限制汇总**：
| 状态 | UI 显示作废按钮 | 实际可作废 | 条件 |
|------|----------------|-----------|------|
| `draft` | ❌ | - | 不在可作废列表 |
| `sent` | ✅ | ✅ | 无对账交易 |
| `viewed` | ✅ | ✅ | 无对账交易 |
| `partial` | ✅ | ✅ | 无对账交易 |
| `paid` | ✅ | ⚠️ | 无对账交易时可作废 |
| `cancelled` | ❌ | - | 已作废 |

> **⚠️ 重要后果**：作废时**物理删除**所有关联的 Transaction 记录，无法恢复。

### 5.3 恢复流程

```
event: DocumentRestored
    ↓
Listener: RestoreDocument
    ├─ Job: RestoreDocument
    │   └─ 更新 status = 'draft'
    └─ Job: CreateDocumentHistory
```

> **注意**：恢复操作只会将状态重置为 `draft`，**不会恢复**已删除的 Transaction 记录。

### 5.4 approved 状态与作废的边界

由于 `approved` 状态**未实际实现**，其与作废的边界关系为：
- 状态列表中包含 `approved`，但无实际流转逻辑
- 若通过数据库直接修改状态为 `approved`：
  - UI 会显示作废按钮（因为不在 `['cancelled', 'draft']` 列表中）
  - `getStatusLabelAttribute()` 会返回 `status-draft`（default 分支）
  - 作废逻辑与其他状态一致（检查是否有已对账交易）
- **不建议在当前版本使用 approved 状态**

## 6. 部分付款场景处理

### 6.1 部分付款的状态计算

**关键代码**：`app/Jobs/Banking/CreateBankingDocumentTransaction.php:98-119`

```php
$compare = bccomp($amount, $total_amount, $precision);

if ($compare === 1) {
    // 超额支付: 抛出异常
    throw new \Exception(trans('messages.error.over_payment', ...));
} else {
    // compare === 0: 全额支付 → status = 'paid'
    // compare === -1: 部分支付 → status = 'partial'
    $this->model->status = ($compare === 0) ? 'paid' : 'partial';
}
```

### 6.2 部分付款的处理边界

| 场景 | 付款路径 | 处理方式 |
|------|---------|---------|
| 付款金额 < 待付金额 | 手工收款 | 状态设为 `partial`，创建对应金额的 Transaction |
| 付款金额 < 待付金额 | 回调收款 | ❌ 不支持（总是全额） |
| 付款金额 = 待付金额 | 两种路径 | 状态设为 `paid` |
| 付款金额 > 待付金额 | 两种路径 | 抛出 `over_payment` 异常，拒绝支付 |
| 多次部分付款 | 手工收款 | 每次创建独立的 Transaction，直到全额付清 |

### 6.3 已付款金额计算

**关键代码**：`app/Models/Document/Document.php:373-407`

```php
public function getPaidAttribute()
{
    // 状态为 paid 时直接返回全额
    if ($this->status == 'paid') {
        return $this->amount;
    }
    
    // 否则累加所有关联 transactions 的金额
    $paid = 0;
    foreach ($this->transactions as $transaction) {
        $paid += $amount;  // 考虑货币转换
    }
    return $paid;
}
```

### 6.4 approved 状态与部分付款的边界

由于 `approved` 状态**未实际实现**：
- 若单据状态为 `approved`，用户仍可点击"添加付款"按钮（因为 `status != 'paid'`）
- 付款逻辑与其他状态一致，付款后状态会变为 `partial` 或 `paid`
- 没有特殊的审批后付款限制逻辑

## 7. 事件-监听器完整映射

| 事件 | 监听器 | 职责 |
|------|--------|------|
| `DocumentCreated` | `CreateDocumentCreatedHistory` | 记录创建历史 |
| | `IncreaseNextDocumentNumber` | 递增单据编号 |
| | `SettingFieldCreated` | 处理自定义字段 |
| `DocumentMarkedSent` | `MarkDocumentSent` | 标记为已发送 |
| `DocumentSent` | `MarkDocumentSent` | 标记为已发送 |
| `PaymentReceived` | `CreateDocumentTransaction` | 创建银行交易（回调路径） |
| | `SendDocumentPaymentNotification` | 发送付款通知（回调路径） |
| `DocumentCancelled` | `MarkDocumentCancelled` | 作废单据 |
| `DocumentRestored` | `RestoreDocument` | 恢复单据 |
| `DocumentViewed` | `MarkDocumentViewed` | 标记为已查看 |
| | `SendDocumentViewNotification` | 发送查看通知 |

## 8. 关键设计决策分析

### 8.1 两条付款路径的设计考量

**优势**：
1. **职责分离**：手工收款强调灵活性，回调收款强调自动化
2. **可扩展性**：新支付网关只需实现 `PaymentController` 抽象类
3. **审计追踪**：回调路径通过事件层，便于日志和审计

**潜在问题**：
1. **行为不一致**：两条路径的通知策略、金额处理逻辑不同，可能导致用户困惑
2. **代码重复**：`CreateBankingDocumentTransaction` 被两条路径调用，但前置条件不同
3. **测试复杂度**：需要分别测试两条路径的边界条件

### 8.2 approved 状态预留的影响

1. **状态列表膨胀**：包含未实现的状态，增加理解成本
2. **潜在不一致**：若通过数据库直接修改为 `approved`，系统行为未定义
3. **未来兼容**：预留状态为后续审批功能留出了扩展空间

### 8.3 其他风险点

1. **作废时物理删除 Transaction**：可能导致审计追踪中断
2. **恢复时不恢复 Transaction**：数据一致性问题
3. **无事务补偿机制**：事件监听失败时可能导致状态不一致
4. **单式记账限制**：无法直接生成标准的资产负债表

### 8.4 优化建议

1. 统一两条付款路径的入口，通过参数区分调用来源
2. 作废时采用**软删除** Transaction 而非物理删除
3. 明确标记 `approved` 为预留状态，或补充实现逻辑
4. 增加**状态机**组件，显式定义合法的状态流转路径
5. 引入**Saga 模式**处理分布式事务一致性

## 9. 代码参考位置汇总

| 模块 | 文件路径 |
|------|---------|
| Document 模型 | `app/Models/Document/Document.php` |
| 创建单据 Job | `app/Jobs/Document/CreateDocument.php` |
| 作废单据 Job | `app/Jobs/Document/CancelDocument.php` |
| 创建银行交易 Job | `app/Jobs/Banking/CreateBankingDocumentTransaction.php` |
| 更新银行交易 Job | `app/Jobs/Banking/UpdateBankingDocumentTransaction.php` |
| 事件配置 | `app/Providers/Event.php` |
| 销售单据控制器 | `app/Http/Controllers/Sales/Invoices.php` |
| 付款模态框控制器 | `app/Http/Controllers/Modals/DocumentTransactions.php` |
| 付款 API 控制器 | `app/Http/Controllers/Api/Document/DocumentTransactions.php` |
| 支付控制器抽象类 | `app/Abstracts/Http/PaymentController.php` |
| 创建交易监听器 | `app/Listeners/Document/CreateDocumentTransaction.php` |
| 付款通知监听器 | `app/Listeners/Document/SendDocumentPaymentNotification.php` |
| Documents Trait | `app/Traits/Documents.php` |
| Transaction 模型 | `app/Models/Banking/Transaction.php` |
| PaymentReceived 事件 | `app/Events/Document/PaymentReceived.php` |
